# 个性化规划与结果页优化方案

## 1. 方案目标

Workbuddy 已经完成了个性化规划模块的第一版，包括：

- 结构化用户画像；
- 偏好问卷；
- 收藏和不感兴趣反馈；
- 用户偏好权重增量更新；
- 个性化候选排序；
- 已体验景点降权；
- 推荐日志；
- 个性化推荐理由；
- 用户画像和效果统计页面；
- 高德 POI、天气、路线和本地攻略事实校验。

下一阶段的目标不是继续堆叠 LLM 文案，而是：

1. 修正个性化链路中的数据口径和可追溯性问题；
2. 将已有真实数据完整展示在结果页；
3. 把每日行程从字段罗列升级为可执行时间轴；
4. 明确区分已验证事实、系统计算、模型建议和待核实内容；
5. 让结果页能够体现个性化工作的技术含量和创新价值。

## 2. 当前系统判断

### 2.1 已有个性化能力

当前系统已经形成以下主链路：

```text
偏好问卷/历史行程/用户反馈
    -> 结构化用户画像
    -> 偏好权重和置信度
    -> POI 候选确定性排序
    -> ReAct Agent 生成行程
    -> 个性化推荐理由和推荐日志
```

后端已经有以下核心实现：

- `UserProfileService`：画像、偏好和反馈更新；
- `SpotTagMapper`：景点、餐厅到偏好标签的确定性映射；
- `PersonalizedRankingService`：候选排序、负反馈降权和历史新颖性处理；
- `RecommendationService`：推荐理由回填和推荐日志；
- `ItineraryValidator`：真实性、预算、天气、去重和场所类型校验；
- `MapEnrichmentService`：高德地址、坐标、图片和路线补全。

### 2.2 当前主要问题

#### 问题一：候选排序和最终结果之间存在断层

当前流程大致为：

```text
高德 POI 候选
    -> PersonalizedRankingService 排序
    -> 排序后的候选交给 LLM
    -> LLM 生成最终行程
    -> RecommendationService 对最终景点重新判断是否命中偏好
```

候选排序目前主要通过改变 POI 列表顺序影响模型选择，最终结果仍可能选择后面的候选。因此当前更准确的描述是“个性化排序影响模型输入”，而不是完全控制最终推荐结果。

建议保存候选阶段的完整证据：

```text
候选名称
候选原始顺序
个性化得分
命中标签
新颖性得分
是否触发回避规则
过滤原因
最终是否入选
```

#### 问题二：排序服务和推荐日志的评分口径不完全一致

`PersonalizedRankingService` 会考虑偏好匹配、负反馈和历史新颖性，但 `RecommendationService` 重新计算时存在以下差异：

- 新颖性得分固定为 `1.0`；
- 距离惩罚固定为 `0.0`；
- 只处理景点，不处理餐厅；
- 只读取景点风格偏好；
- 没有复用候选阶段的真实得分。

这会导致数据库中的推荐日志更像事后标签，而不是完整记录真实排序过程。

建议抽取统一评分组件：

```text
PersonalizedScoreCalculator
    -> PersonalizedRankingService 复用
    -> RecommendationService 复用
```

#### 问题三：负反馈可能造成标签过度泛化

一个地点可能同时映射为多个标签。用户不喜欢某个商场，不一定代表用户不喜欢购物、城市漫游或拍照。

建议区分反馈粒度：

- 不喜欢这个具体地点；
- 不喜欢这类地点；
- 不喜欢这个主题标签；
- 距离太远；
- 太拥挤；
- 价格太高；
- 不适合当前节奏。

只有用户明确选择“不喜欢这类景点”时，才降低对应标签权重。

#### 问题四：收藏信号还可以更精细

收藏应同时记录：

- 具体 POI 收藏；
- 同类标签收藏；
- 城市上下文；
- 收藏发生时的旅行请求。

下一次推荐可以提高相似新地点的分数，但不应简单重复推荐已经收藏过的同一景点。

#### 问题五：已体验判断主要依赖名称

仅通过景点名称判断已体验，可能无法识别名称变体、分店、景区入口和 POI 别名。

建议优先使用以下顺序：

```text
poi_id 相同
    -> 城市 + 标准化名称相同
    -> 经纬度距离小于阈值
    -> 名称模糊匹配兜底
```

同时区分：

- 已生成；
- 已保存；
- 已收藏；
- 用户实际体验过。

当前系统更准确地表示“历史行程中出现过”，不应直接表述成“用户已经实际去过”。

#### 问题六：不同生成入口需要统一收尾

流式生成入口已经执行推荐理由回填和推荐日志写入，但非流式生成入口也应保证相同语义。

建议抽取统一收尾服务：

```text
TripGenerationFinalizer
```

统一负责：

1. 推荐理由回填；
2. 推荐日志写入；
3. 画像版本和排序版本记录；
4. source notes 汇总；
5. 缓存前的最终处理。

#### 问题七：缓存必须严格绑定画像版本

个性化反馈后，同样的旅行参数不能继续命中旧结果。缓存 key 至少应包含：

```text
userId
profileVersion
destination
startDate
endDate
travelers
budget
preferences
pace
dietaryPreferences
hotelLevel
specialNotes
rankingVersion
```

需要重点验证：

- 点击“不感兴趣”后是否绕过旧缓存；
- 修改问卷后是否重新生成；
- 修改排序规则后是否隔离旧缓存；
- 缓存命中结果是否保留当前版本的推荐理由。

## 3. 结果页内容不足的根本原因

结果页已经包含概览、Agent 轨迹、数据来源、预算、地图、天气、按天花费、地图点位、反馈和评分等模块。

但当前每日行程部分主要展示：

```text
day.spots[0]
day.meals[0]
day.transport[0]
day.notes[last]
```

这会导致真实数据已经生成，但页面只展示每天的第一处景点、第一项餐饮和第一条交通信息。

因此最优先的改造不是调用更多 LLM，而是完整展示已有数据。

## 4. 结果页总体信息架构

建议将结果页组织为：

```text
第一屏：行程决策摘要
    -> 日期、人数、预算、节奏、天气、个性化概览

第二屏：每日完整时间轴
    -> 景点、交通、餐饮、住宿、备注和证据来源

第三屏：地图与路线
    -> 每日点位、路线、距离和交通时间

第四屏：预算与天气
    -> 总预算、按天预算、预算使用率、天气影响

第五屏：可信度与个性化依据
    -> 数据来源、推荐理由、过滤原因、Agent 轨迹、用户反馈
```

## 5. 结果页应增加的有效信息

### 5.1 完整每日时间轴

每一天展开后展示全部真实安排：

```text
第 1 天 · 历史文化探索
2026-09-10

09:00 - 11:30
故宫博物院
地址：北京市东城区景山前街4号
门票：¥60
来源：高德 POI / 本地攻略
推荐理由：匹配你的历史文化偏好

11:30 - 12:00
故宫博物院 -> 王府井
交通：驾车
距离：4.2 km
预计：18 分钟
来源：高德路线估算

12:00
午餐：本地餐厅
人均：¥80
来源：高德 POI

14:00 - 17:00
景山公园
地址：...
门票：...
```

可直接使用现有字段：

- `start_time`；
- `end_time`；
- `spots`；
- `meals`；
- `transport`；
- `hotel`；
- `notes`；
- `source`；
- `personal_note`。

时间轴排列建议由代码完成：

1. 景点按照 `start_time` 排序；
2. 交通插入相邻景点之间；
3. 餐饮按照 `meal_type` 插入；
4. 住宿放在当天末尾；
5. 没有时间的内容放入“其他安排”，不编造时间。

### 5.2 每日概览统计

每天顶部增加确定性统计：

```text
景点：3 个
餐饮：2 项
交通：4 段
预计交通：8.6 km / 52 分钟
当日花费：¥680
预计游览时长：6.5 小时
```

字段计算方式：

- 景点数：`spots.length`；
- 餐饮数：`meals.length`；
- 交通距离：累加 `distance_km`；
- 交通时间：累加 `estimated_minutes`；
- 当日费用：使用现有预算计算逻辑；
- 游览时长：根据完整的 `start_time` 和 `end_time` 计算。

如果缺少时间字段，则不展示预计游览时长，不让 LLM 猜测。

### 5.3 预算健康度

现有预算明细可以扩展为：

```text
预算使用率：82%
剩余预算：¥720
人均每日预算：¥426
最大支出：住宿
预算状态：在计划范围内
```

这些内容全部由代码计算：

$$
预算使用率 = \frac{estimatedBudget}{requestedBudget}
$$

$$
剩余预算 = requestedBudget - estimatedBudget
$$

$$
人均每日预算 = \frac{estimatedBudget}{travelers \times days}
$$

如果前端没有原始请求预算，建议在行程响应中增加：

```text
requested_budget
travelers
trip_days
```

不要让 LLM 生成预算评价，金额以系统预算明细为唯一依据。

### 5.4 数据可信度总览

建议增加“数据可信度概览”：

```text
真实 POI：8 项
本地攻略：4 项
高德路线：6 段
模型建议：1 项，需核实
地址已校验：7 项
```

数据可以通过代码统计 `spots`、`meals`、`hotel` 和 `transport` 的 `source` 字段。

建议颜色和标签：

| 类型 | 展示样式 |
|---|---|
| 高德 POI | 已验证 |
| 本地攻略 | 已验证 |
| 高德路线估算 | 已验证/系统估算 |
| 估算（LLM） | 模型建议 |
| LLM 建议（需核实） | 待核实 |

### 5.5 个性化规划说明

在结果页顶部增加确定性的个性化摘要：

```text
本次规划结合了：

✓ 你的历史文化偏好
✓ 你常用的轻松节奏
✓ 你的少辣饮食要求
✓ 已减少历史行程中重复出现的景点
✓ 已根据天气和真实路线调整安排
```

建议由后端返回结构化数据：

```json
{
  "personalization_summary": {
    "matched_preferences": ["历史文化", "轻松"],
    "applied_constraints": ["少辣", "预算 3200 元"],
    "novelty_note": "已减少历史行程中出现过的景点",
    "evidence_level": "PROFILE_AND_BEHAVIOR"
  }
}
```

前端根据结构化字段展示，不让 LLM 自由编写这段说明。

### 5.6 为什么没有推荐某些内容

这是非常适合答辩展示的能力。示例：

```text
本次未安排部分户外景点，原因：

- 9 月 12 日天气预报为大雨；
- 已优先替换为室内景点；
- 用户画像中存在“少走路”偏好；
- 该景点距离当日其他安排较远；
- 用户曾对同类景点标记不感兴趣。
```

建议新增结构化字段：

```json
{
  "filtered_candidates": [
    {
      "name": "某户外景区",
      "reason": "恶劣天气下不建议安排户外活动",
      "evidence": "天气预报",
      "severity": "HARD"
    }
  ]
}
```

原因与证据来源应明确对应：

| 原因 | 证据来源 |
|---|---|
| 大雨或雷暴 | 天气 API |
| 少走路 | 用户画像 |
| 距离较远 | 高德坐标或路线 |
| 不感兴趣 | 用户行为 |
| 室内替换 | 系统规则 |
| 预算超限 | 系统预算计算 |

### 5.7 交通可执行性

在每日时间轴中展示交通段：

```text
故宫博物院 -> 王府井
驾车 · 4.2 km · 18 分钟
来源：高德路线估算
```

每天再汇总：

```text
今日交通：4 段
总距离：12.8 km
总耗时：58 分钟
```

可以根据确定性规则给出节奏标签：

```text
交通时间较短 + 景点数量较少 = 轻松
中等数量和交通 = 适中
景点较多 + 交通时间较长 = 紧凑
```

这类标签是系统计算结果，不应由 LLM 生成。

### 5.8 酒店实用信息

增加住宿信息：

```text
酒店：XXX
档次：舒适型
地址：...
每晚：¥360
连续入住：2 晚
与主要景点距离：约 4.8 km
数据来源：高德 POI
```

已有酒店名称、等级、地址、经纬度和价格，可以通过代码统计连续入住晚数。酒店与景点的距离也可以使用已有坐标和位置校验结果。

### 5.9 餐饮信息

完整展示当天所有餐饮：

```text
午餐：某餐厅
人均：¥80
备注：本地特色菜
来源：高德 POI
```

“少辣适配”等信息只有在用户明确提出，或餐厅数据、攻略卡片存在明确证据时才能展示，否则应写成：

```text
饮食适配：待核实
```

不能根据餐厅名称自行推断饮食适配。

## 6. 信息可信度分层

这是防止幻觉的核心设计。

### 6.1 已验证事实

```text
景点名称：高德 POI / 本地攻略
地址：高德 POI
坐标：高德 POI
路线距离：高德路线
天气：天气 API
门票：本地攻略卡片
```

### 6.2 系统计算结果

```text
当日费用
总预算
预算使用率
交通总时长
景点数量
个性化得分
推荐命中率
```

### 6.3 模型建议

```text
景点描述
游玩建议
餐厅推荐说明
旅行提示
行程主题
```

### 6.4 待核实内容

```text
没有在 POI 或攻略中命中的名称
模型生成但没有真实来源的价格
无法匹配的地址
估算（LLM）交通
```

页面建议提供图例：

```text
已验证事实 | 系统计算 | 模型建议 | 待核实
```

## 7. LLM 使用边界

### 7.1 不应交给 LLM 的内容

| 内容 | 推荐来源 |
|---|---|
| 景点名称 | 高德 POI / 攻略库 |
| 餐厅名称 | 高德 POI |
| 酒店地址 | 高德 POI |
| 经纬度 | 高德 POI |
| 交通距离 | 高德路线 |
| 交通时长 | 高德路线 |
| 天气 | Open-Meteo / 高德 |
| 门票 | 攻略卡片，缺失则待核实 |
| 总预算 | 后端计算 |
| 当日预算 | 后端计算 |
| 个性化得分 | 确定性算法 |
| 推荐标签 | `SpotTagMapper` |
| 已体验判断 | 历史行程和 POI ID |
| 预算使用率 | 前端或后端计算 |
| 景点数量 | 代码统计 |
| 交通总距离 | 代码累加 |
| 推荐命中率 | 推荐日志统计 |

### 7.2 适合交给 LLM 的内容

- 从用户自然语言中提取偏好；
- 规划整体行程主题；
- 基于真实候选生成自然语言说明；
- 补充游览顺序建议；
- 生成旅行摘要和注意事项。

所有没有事实来源的 LLM 内容都应标记为：

```text
模型建议
```

或：

```text
需核实
```

## 8. 后端改造建议

### 8.1 统一评分计算

新增：

```text
PersonalizedScoreCalculator
```

输出统一结构：

```json
{
  "item_name": "杜甫草堂",
  "preference_score": 0.81,
  "behavior_score": 0.72,
  "novelty_score": 1.0,
  "distance_penalty": 0.10,
  "final_score": 0.84,
  "matched_tags": ["历史文化"],
  "avoid_reason": null,
  "explanation": "符合历史文化偏好，且历史行程中尚未出现"
}
```

排序服务和推荐日志服务共同复用该结构。

### 8.2 增加版本追踪

推荐日志建议增加：

```text
profile_version
ranking_version
model_version
data_snapshot_time
```

这样可以追踪每次推荐对应的用户画像、算法和数据状态。

### 8.3 增加过滤候选记录

建议增加：

```text
filtered_candidates
constraint_notes
```

记录候选项被过滤的原因、证据和约束级别，支持结果页解释和答辩展示。

### 8.4 统一生成收尾

抽取 `TripGenerationFinalizer`，让流式和非流式生成入口执行一致的：

- 推荐理由回填；
- 推荐日志写入；
- 可信度信息汇总；
- 个性化摘要构建；
- 缓存前最终处理。

## 9. 前端改造建议

### P0：完整展示已有数据

1. 每日行程展示全部景点；
2. 展示全部餐饮；
3. 展示全部交通段；
4. 展示每个景点的起止时间；
5. 展示每个项目的数据来源；
6. 将每日行程改为完整时间轴。

### P1：增加确定性信息面板

1. 行程总览统计；
2. 预算使用率和剩余预算；
3. 每日景点、餐饮、交通数量；
4. 总交通距离和耗时；
5. 数据可信度统计；
6. 个性化规划说明；
7. 天气影响说明。

### P2：完善个性化证据

1. 统一排序服务和推荐日志的评分口径；
2. 推荐日志记录画像版本和算法版本；
3. 记录候选过滤原因；
4. 增加“为什么没有推荐”；
5. 反馈增加原因选择；
6. 区分具体 POI 负反馈和类别负反馈。

### P3：增强答辩表现

1. 展示候选排序解释；
2. 展示个性化前后对比；
3. 展示反馈前后画像权重变化；
4. 展示相同请求在反馈前后的结果差异；
5. 展示个性化指标趋势；
6. 完成三组方案对照实验。

## 10. 推荐的答辩演示流程

准备一个固定用户画像：

```text
历史文化
美食
轻松节奏
少辣
避免重复景点
```

第一次生成行程：

```text
推荐历史文化景点和少辣餐饮
```

然后对某类景点进行“不感兴趣”反馈，展示画像变化：

```text
购物标签权重：0.62 -> 0.32
行为反馈来源：新增
画像版本：v3 -> v4
```

再次使用相同城市和相近条件生成：

```text
缓存失效
候选重新排序
购物类景点降低优先级
新的历史文化景点进入行程
```

结果页展示：

```text
为什么推荐：匹配历史文化偏好

为什么没有推荐：根据你的负反馈降低购物类景点优先级

数据依据：高德 POI + 本地攻略 + 用户画像 + 历史反馈
```

## 11. 最终建设目标

系统最终应从“AI 生成一份旅行计划”升级为：

> 一个融合用户画像、行为反馈、真实 POI、天气、路线和预算约束，并能够解释推荐依据和数据可信度的个性化旅游规划系统。

核心原则是：

```text
真实数据负责事实
确定性代码负责计算和约束
推荐算法负责个性化排序
LLM 负责理解、规划和表达
用户反馈负责持续更新画像
```

这样既能显著丰富结果页面，也能避免无依据的 LLM 内容，同时能够充分展示个性化模块的工程工作量、算法工作量和可验证性。
