# AI 旅游助手系统产品化功能审查报告

> 审查日期：2026-09-07  
> 审查范围：首页、景点推荐、景点详情、收藏、用户画像、社区帖子、互动、审核、图片上传、城市专题、关注、推荐监控和 A/B 实验  
> 审查方式：源码审查、数据库脚本审查、后端打包、后端全量测试、前端生产构建

## 一、审查结论

Workbuddy 已经把系统从“AI 行程规划器”扩展成了一个具备平台雏形的旅行系统，当前已经覆盖：

- Dashboard 首页；
- 推荐景点列表和景点详情；
- 景点收藏、不感兴趣和画像反馈；
- 景点按需同步和 RAG 内容增强；
- 用户帖子、帖子详情和我的帖子；
- 点赞、收藏、不感兴趣；
- 评论、回复和举报；
- 帖子待审核、通过、拒绝、隐藏；
- 管理后台；
- 图片上传；
- 城市专题；
- 用户旅行主页和关注关系；
- 推荐曝光日志；
- A/B 实验和推荐监控；
- 审计日志和内容质量评分。

产品功能已经比较丰富，当前主要问题不再是“有没有功能”，而是：

```text
数据状态是否一致
权限和默认凭据是否安全
已发布内容修改后是否重新审核
旧数据库升级是否可靠
推荐统计是否代表真实含义
边界异常是否有完整测试
```

总体判断：

> P0 景点发现链路基本可用；社区主链路可以演示；正式使用前必须优先修复内容修改绕过审核、默认管理员凭据、默认 JWT 密钥和数据库升级失败仍继续启动这几个问题。

---

## 二、验证结果

### 2.1 后端

执行：

```text
mvn -q -DskipTests package
```

结果：通过，退出码为 `0`。

执行后端全量测试：

```text
mvn test -q
```

结果：命令完成且退出码为 `0`，当前测试没有阻断构建。

### 2.2 前端

执行：

```text
npm run build
```

结果：通过，`vue-tsc --noEmit` 和 Vite 构建均通过。

构建同时产生 chunk 过大的警告：

```text
index.js 约 1.7 MB，超过 500 KB 建议阈值
```

这不是当前功能 bug，但后续应通过路由懒加载和 `manualChunks` 优化首屏加载。

### 2.3 启动状态

当前已有一次 `TripPlannerApplication` 启动进程以退出码 `1` 结束，但终端上下文没有保留完整异常堆栈，因此暂时不能把具体原因归因到某个类。

建议下一次启动时保留完整日志，重点查看：

- MySQL 连接失败；
- Redis 连接失败；
- `SchemaAutoUpgrade` 自动升级失败；
- 端口 8080 占用；
- 管理员初始化失败；
- `schema.sql` 初始化失败。

后端可以打包成功并不代表应用能连接当前本机数据库和 Redis。

---

## 三、问题清单

严重性分级：

- **P0：阻塞正式使用或存在明显安全/内容风险**；
- **P1：高优先级功能或数据一致性问题**；
- **P2：体验、性能、可维护性和统计质量问题**；
- **P3：后续增强项**。

---

## P0 问题

### P0-1 已发布帖子编辑后不会重新审核

**位置：** `backend/src/main/java/com/yuntu/tripplanner/service/PostService.java` 的帖子更新逻辑。

当前注释和实现允许：

```text
PUBLISHED -> 作者直接编辑 -> 仍然 PUBLISHED
```

代码只对 `REJECTED` 做了重新回到草稿处理，已发布内容修改后没有变成 `PENDING_REVIEW`。

这会绕过现有的：

- 敏感词检查；
- 广告和联系方式检查；
- 重复内容检查；
- 质量评分；
- 管理员人工审核。

攻击或误操作路径：

```text
帖子首次审核通过
    -> 作者修改正文或封面
    -> 直接保持公开
    -> 修改后的内容没有再次审核
```

**建议：**

- `PUBLISHED` 修改标题、摘要、正文、封面、城市、关联景点时，自动变成 `PENDING_REVIEW`；
- 保存成功后对外继续展示旧版本，或直接进入不可见状态，二选一；
- 推荐做“版本化”：`post_revision` 保存待审版本，审核通过后切换公开版本；
- 最小实现可以先采用：已发布帖子编辑后进入 `PENDING_REVIEW`，前端提示“修改后需重新审核”。

**必须补测：**

```text
PUBLISHED 修改正文 -> PENDING_REVIEW
PUBLISHED 修改封面 -> PENDING_REVIEW
PUBLISHED 未修改提交 -> 不重复创建审核任务
```

---

### P0-2 默认管理员账号和密码仍可直接使用

**位置：** `backend/src/main/resources/application.yml`、`AdminInitializer.java`。

当前默认配置类似：

```yaml
community:
  admin-username: ${COMMUNITY_ADMIN_USERNAME:admin}
  admin-password: ${COMMUNITY_ADMIN_PASSWORD:admin123}
```

如果部署时没有覆盖环境变量，应用会创建：

```text
admin / admin123
```

这在答辩演示环境可以接受，但不能作为默认生产行为。只要服务暴露到网络，管理员账号就会成为明显攻击入口。

**建议：**

- 开发环境可以保留默认值，但必须在生产 profile 启动时拒绝默认密码；
- 或第一次启动随机生成管理员密码并只输出一次；
- 或要求 `COMMUNITY_ADMIN_PASSWORD` 必须存在，否则管理员初始化失败并阻止启动；
- 前端后台入口隐藏不是安全措施，服务端角色校验仍必须保留。

---

### P0-3 默认 JWT 密钥仍然是公开固定值

**位置：** `backend/src/main/resources/application.yml`。

当前存在默认 JWT secret：

```yaml
jwt:
  secret: ${JWT_SECRET:ai-travel-assistant-demo-jwt-secret-key-change-in-production}
```

注释虽然提醒生产覆盖，但只要忘记配置，所有部署实例都使用同一个公开密钥。攻击者可以伪造 token，具体风险包括：

- 冒充其他用户；
- 读取其他用户行程和画像；
- 修改帖子；
- 调用管理员接口，前提是能构造管理员身份 claims；
- 访问用户收藏和推荐数据。

**建议：**

- 非 `local` profile 禁止使用默认 secret；
- 启动时校验 secret 长度和是否仍为 demo 值；
- 生产部署必须通过环境变量或密钥管理服务注入；
- 不要仅依赖 README 提示。

---

### P0-4 SchemaAutoUpgrade 失败后仍允许应用继续启动

**位置：** `backend/src/main/java/com/yuntu/tripplanner/config/SchemaAutoUpgrade.java`。

当前自动升级失败后只是：

```text
记录 warn 日志
继续启动应用
```

这会导致：

```text
旧库缺 role/quality_score/ab_variant
    -> ALTER 失败
    -> 应用继续启动
    -> 相关接口运行时才报 Unknown column
```

这种状态比启动失败更难排查，因为健康检查可能显示服务在线，但社区审核或推荐监控接口才会报错。

**建议：**

- 生产 profile 中升级失败直接抛异常阻止启动；
- 或增加 `schema.auto-upgrade-strict=true` 配置；
- 自动升级前后输出数据库版本信息；
- 关键表和关键列增加启动健康检查；
- 长期应使用 Flyway/Liquibase，而不是继续堆叠启动期 ALTER。

---

## P1 问题

### P1-1 已发布内容的“公开版本”和“编辑版本”没有分离

这是 P0-1 的数据模型根因。

目前 `travel_post` 只有一份正文、封面和状态。如果要做到：

```text
审核期间用户可以继续编辑
公开用户继续看到旧版本
```

单表状态机不够，建议增加：

```text
post_revision
- id
- post_id
- version
- title
- summary
- content
- cover_image
- city
- spots_json
- status
- submitted_at
- reviewed_at
- reviewed_by
```

第一版也可以暂时采用简单策略，但要在产品上明确：已发布帖子编辑后立即下架并重新审核。

---

### P1-2 帖子封面仍允许任意外部 URL

虽然已经有 `/file/upload-image` 和本地上传服务，但 `PostService` 的创建和更新仍直接接受 `coverImage` 字符串。

这意味着用户可以提交：

- 任意外部图片 URL；
- 失效 URL；
- 非图片 URL；
- 极长字符串；
- 指向不受控制站点的资源。

浏览器展示本身不等于服务端 SSRF，但会带来：

- 图片失效；
- 版权不可追踪；
- 第三方资源污染页面；
- 隐私和安全策略不可控。

**建议：**

- 只接受 `/uploads/` 下由本系统生成的 URL；
- 或只允许已知高德图片域名；
- 封面上传完成后返回资源 ID，帖子只保存资源 ID；
- 删除帖子时决定是否清理未被其他内容引用的文件；
- 对图片做尺寸、解码和内容审核，而不只检查文件头。

---

### P1-3 本地上传目录不适合多实例部署

当前图片保存到：

```text
./uploads
```

并通过 `/uploads/**` 静态映射访问。

单机演示没有问题，但如果部署到：

- Docker 容器；
- 多实例；
- Azure App Service；
- Kubernetes；

本地文件可能随容器重建丢失，或不同实例之间图片不一致。

**建议：**

- 当前答辩环境可以保留本地磁盘；
- 正式部署改用 Azure Blob Storage、S3 或 MinIO；
- 数据库保存资源 key，不保存实例相关绝对路径；
- 增加上传文件清理和孤儿资源扫描任务。

---

### P1-4 帖子互动和画像更新采用“互动成功、画像失败也继续”的非原子策略

**位置：** `PostInteractionService.recordPersonalizationBehavior`。

该方法捕获所有异常后只写日志：

```text
互动成功
画像更新失败
接口仍返回成功
```

这与景点收藏的“收藏和画像同事务”策略不一致。

这种取舍不是绝对错误：内容互动本身可以优先可用。但必须明确产品语义，否则用户会看到“已收藏，系统会记住”，实际画像没有变化。

**建议二选一：**

- 强一致：互动和画像更新放进同一事务，失败整体回滚；
- 最终一致：互动成功后写 `personalization_event`，异步重试画像更新，并在日志中记录待处理状态。

不建议当前这种“吞掉异常且没有补偿任务”的中间状态。

---

### P1-5 推荐曝光日志缺少幂等键，刷新页面会重复计算曝光

帖子推荐在每次个性化请求时写 `post_feed_log`，景点推荐也会写 `spot_feed_log`。当前没有看到“同一请求/同一页”的请求 ID 或去重键。

用户刷新页面、重复进入页面或前端重试时，会产生多次曝光：

```text
一次真实展示
    -> 多条曝光日志
    -> 曝光分母变大
    -> 收藏率/不感兴趣率被稀释
```

**建议：**

增加：

```text
request_id
session_id
feed_trace_id
```

或者用以下组合做幂等约束：

```text
(user_id, feed_request_id, item_id)
```

同时区分：

- API 返回；
- 实际进入视口；
- 用户点击；
- 用户收藏。

当前日志更接近“返回曝光”，不应直接称为真实浏览曝光。

---

### P1-6 推荐监控的景点反馈关联依赖 poi_id，缺失 poi_id 时统计失真

`FeedMonitorService` 使用：

```text
user_id + poi_id
```

关联景点曝光和 `user_behavior`。

如果某个景点没有 `poi_id`，即使用户实际收藏或不感兴趣，也不会进入反馈漏斗。景点底座设计允许 `poi_id` 为空，因此这条路径确实存在。

**建议：**

- 曝光日志和行为都优先记录 `spot_id`；
- `poi_id` 作为辅助关联键；
- 监控关联顺序：`spot_id` > `poi_id` > 规范化名称；
- 前端景点反馈请求使用系统 `spot_id`，不要只传高德 POI ID。

---

### P1-7 景点推荐的热门语义和实际数据仍需要统一

当前景点推荐的 `popular` 主要按攻略质量排序，而不是收藏量、浏览量或生成次数。

帖子热门排序已经使用：

```text
点赞 + 2 × 收藏 + 评论
```

两条推荐流的“热门”语义不一致。

**建议：**

- 景点没有热度数据时将 UI 文案改为“攻略优先”；
- 或给景点增加 `favorite_count`、`trip_mention_count`、`view_count`；
- 之后统一热门分和时间衰减规则；
- 不要让用户把“数据质量高”误解为“全站热门”。

---

## P2 问题

### P2-1 景点同步需要真正的过期时间判断

当前已经支持已有 POI 重新做 RAG 增强，但需要确认同步触发条件。推荐逻辑不应只判断“城市是否已有足够数量”，否则一个城市达到阈值后可能永远不刷新。

建议条件：

```text
景点数量 < 20
或城市最近同步时间超过 7 天
```

同时增加 Redis 锁，避免多个用户同时访问新城市时重复调用高德。

---

### P2-2 详情相关推荐存在重复读取画像的风险

景点详情组装相关推荐时，如果每个相关景点都重新查询用户偏好，会产生 N+1 查询。

建议：

```text
进入 detail 时读取一次 prefs
    -> relatedSpots 复用同一份 prefs
```

同理，收藏状态也可以一次查询当前用户的收藏 ID 集合，而不是逐条查。

---

### P2-3 首页和社区页面依赖固定城市列表

前端城市选择仍有固定城市常量。后端虽然支持按任意城市调用高德，但用户无法从页面搜索任意城市。

建议后续增加：

- 城市搜索接口；
- 城市校验和标准化；
- 城市搜索结果缓存；
- 空城市和不存在城市的明确提示。

---

### P2-4 前端构建产物过大

当前生产构建 JS 约 1.7 MB，CSS 约 73 KB。

建议：

- 路由组件使用动态 `import()`；
- 将管理后台、编辑器、地图和图表拆为异步 chunk；
- 配置 Vite `manualChunks`；
- 检查 Ant Design Vue 和地图依赖是否被首屏全部加载；
- 首页先加载 Dashboard，再延迟加载社区和管理模块。

---

### P2-5 公开内容浏览计数不包含匿名用户

帖子详情中的浏览计数逻辑只对登录用户且非作者增加：

```text
viewerId != null && !mine
```

当前全局 JWT 拦截器又要求除 `/auth/**` 外全部登录，因此实际上匿名访问被整体挡住。但如果后续把社区公开流改为匿名可访问，浏览统计口径需要重新设计。

建议提前明确：

- 社区是否允许匿名浏览；
- 匿名浏览是否写设备/会话级去重事件；
- 计数是否采用异步事件，避免详情接口直接更新计数。

---

### P2-6 启动配置和文档存在环境要求差异

系统依赖：

- MySQL 3310；
- Redis 6379；
- 可选 Chroma 8000；
- LLM API Key；
- 高德 API Key；
- 管理员账号配置；
- 上传目录权限。

应用打包通过不代表启动环境满足这些依赖。建议增加：

```text
/actuator/health 或自定义 /system/health
```

至少输出：

- 数据库连接状态；
- Redis 状态；
- RAG 数据加载状态；
- 上传目录可写状态；
- 外部 API 配置是否存在，但不要输出密钥。

---

## 四、实现中做得比较好的部分

### 4.1 景点 P0 链路已经闭环

```text
高德按需同步
    -> spot 景点底座
    -> RAG 简介增强
    -> 个性化排序
    -> 景点详情
    -> 收藏/不感兴趣
    -> 画像反馈
```

同时已有：

- `spot_id` 稳定 ID；
- `poi_id` 来源 ID；
- `data_quality` 数据质量标记；
- RAG 未命中的诚实兜底文案；
- 收藏唯一键；
- 不存在景点 404；
- 并发重复收藏幂等。

### 4.2 社区公开流状态过滤清楚

公开流只查询 `PUBLISHED`，待审、拒绝、隐藏和删除内容不会进入公开流，这是正确的安全边界。

### 4.3 管理接口有服务端角色校验

审核和举报处理都通过 `requireAdmin` 校验，前端隐藏按钮不是唯一权限措施，方向正确。

### 4.4 内容规则和质量评分已分层

内容检查、重复检测和质量评分不直接替代人工审核，而是作为提交审核前的自动关卡或审核辅助，这个职责划分合理。

### 4.5 推荐日志和算法版本意识较好

推荐日志已经记录画像版本和排序版本，帖子流还有曝光记录和 A/B 变体字段，为后续答辩和实验提供了可追踪基础。

---

## 五、建议的修复顺序

### 第一批：正式使用前必须处理

1. 已发布帖子修改后重新进入审核；
2. 禁止生产使用默认管理员密码；
3. 禁止生产使用默认 JWT secret；
4. SchemaAutoUpgrade 关键失败时阻止启动；
5. 补充真实数据库事务和状态机集成测试。

### 第二批：数据和统计一致性

1. 曝光日志增加请求幂等标识；
2. 景点曝光使用 `spot_id` 关联行为；
3. 明确“热门”和“攻略优先”的产品语义；
4. 帖子互动画像更新增加补偿机制；
5. 景点同步增加过期刷新和分布式锁。

### 第三批：性能和体验

1. 详情相关推荐消除 N+1 查询；
2. 增加城市搜索；
3. 首页和管理后台路由懒加载；
4. 图片资源改对象存储；
5. 增加运行健康检查。

---

## 六、建议补充的测试矩阵

### 帖子状态机

| 场景 | 期望 |
|---|---|
| DRAFT 提交 | PENDING_REVIEW |
| REJECTED 修改后提交 | PENDING_REVIEW |
| PENDING_REVIEW 修改 | 只能作者修改，状态仍需重新确认 |
| PUBLISHED 修改正文 | PENDING_REVIEW 或生成待审版本 |
| HIDDEN 普通用户查看 | 404/不可见 |
| HIDDEN 管理员查看 | 可见 |
| DELETE 后公开流 | 不出现 |

### 权限和凭据

| 场景 | 期望 |
|---|---|
| USER 调审核接口 | 403 |
| ADMIN 调审核接口 | 成功 |
| 修改他人帖子 | 403 |
| 默认管理员密码未覆盖的生产启动 | 阻止启动 |
| 默认 JWT secret 的生产启动 | 阻止启动 |

### 推荐统计

| 场景 | 期望 |
|---|---|
| 刷新同一推荐页 | 不重复计入同一次请求曝光 |
| 景点无 poi_id 但有 spot_id | 仍能关联反馈 |
| 热门排序 | 排序依据和 UI 文案一致 |
| 无画像推荐 | 明确返回热门/攻略优先，不伪装个性化 |

### 图片

| 场景 | 期望 |
|---|---|
| 伪造 Content-Type | 拒绝 |
| 非白名单格式 | 拒绝 |
| 超过 5MB | 拒绝 |
| 帖子提交外部任意 URL | 拒绝或明确允许并做域名白名单 |
| 上传目录不可写 | 返回明确错误，不产生数据库脏数据 |

---

## 七、最终评价

当前实现已经具备完整的平台化雏形，甚至提前覆盖了原方案中的阶段三和阶段四部分能力。工程方向总体正确：

```text
功能模块齐全
权限边界基本建立
推荐有日志
内容有审核
画像有反馈闭环
```

但当前最需要做的是收敛质量，而不是继续增加功能。建议暂时停止扩展关注、A/B 和更多运营功能，优先把以下闭环打磨可靠：

```text
发帖
    -> 自动检查
    -> 人工审核
    -> 发布
    -> 用户互动
    -> 推荐曝光
    -> 反馈统计
```

尤其要先解决：

1. 已发布帖子编辑绕过审核；
2. 默认管理员凭据；
3. 默认 JWT 密钥；
4. 数据库升级失败仍继续启动；
5. 推荐曝光和反馈统计的关联口径。

完成这五项后，系统才适合作为一个稳定的“旅行平台”演示，而不只是功能数量较多的原型。
