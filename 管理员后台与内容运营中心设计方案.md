# 管理员后台与内容运营中心设计方案

> 目标：把当前“普通用户工作台上增加一个管理后台按钮”的形态，升级为真正独立的旅行内容运营后台。
>
> 适用对象：Workbuddy 前端与后端实现
>
> 当前系统：Vue 3 + TypeScript + Spring Boot 3 + MyBatis-Plus + MySQL + RAG 攻略库

---

## 一、结论先说

你的判断是正确的。当前系统虽然已经具备管理员权限校验、帖子审核、举报处理、用户列表、推荐监控和 A/B 实验，但管理产品仍存在三个根本问题：

```text
管理员界面仍然套用普通用户 AppLayout
审核帖子点击后进入普通用户帖子详情
举报处理只有举报快照，缺少被举报内容的完整审查上下文
城市专题和本地攻略缺少可运营、可审核、可上下线的内容后台
```

当前实现可以叫“管理员功能集合”，还不能称为成熟的“管理后台”。

建议下一阶段把产品拆成两个明确的工作空间：

```text
普通用户端：发现、规划、收藏、社区、个人空间
管理员端：内容审核、举报治理、城市内容、景点数据、推荐运营、用户治理、系统审计
```

管理员登录后，不应该只是看到普通用户导航旁边多一个“管理后台”按钮，而应该进入独立的管理控制台，使用完全不同的信息架构、布局密度和操作语义。

---

## 二、当前实现问题审查

### P0-1 管理员仍使用普通用户布局

当前路由结构是：

```text
/                         普通用户 AppLayout
/moderation               普通用户 AppLayout 下的管理页面
```

当前 `AppLayout.vue` 仍然承载：

- 首页；
- 规划；
- 发现；
- 社区；
- 我的；
- 管理后台按钮。

这会带来几个问题：

- 管理员进入后台后仍然看到普通用户语义；
- 普通用户导航和运营导航混在一起；
- 管理员很难理解当前是在“审核”还是“浏览社区”；
- 后续增加城市运营、景点治理、数据监控时，顶部导航会继续膨胀；
- 管理员容易误把普通用户详情页当作审核工作区。

### 建议

增加独立的 `AdminLayout.vue`：

```text
/admin                       管理后台首页
/admin/content/review        内容审核
/admin/reports               举报中心
/admin/posts                 帖子治理
/admin/guides                攻略内容
/admin/cities                城市专题
/admin/spots                 景点数据
/admin/recommendations       推荐运营
/admin/users                 用户治理
/admin/audit                 审计日志
/admin/settings              系统设置
```

`/moderation` 可以保留为旧地址兼容入口，但访问后重定向到 `/admin/content/review`。

管理员端应使用：

- 左侧固定后台导航；
- 顶部工作区标题、面包屑和当前管理员信息；
- 独立的后台色彩和密度；
- 全局待处理数量；
- 快捷搜索；
- 最近操作；
- 退出管理后台，而不是直接混在普通用户退出按钮里。

---

### P0-2 审核帖子点击后进入普通用户帖子详情

当前审核页的 `goPost()` 类似：

```ts
router.push({ name: "post-detail", params: { id: String(p.id) } });
```

这会打开普通用户使用的 `PostDetail.vue`。它的目标是阅读和互动，不是审核。

审核员需要看到的内容应该不同：

```text
帖子当前状态
提交时间和更新时间
作者信息与历史违规记录
正文、封面、关联城市和景点
自动规则命中结果
质量评分拆解
重复内容检测结果
用户举报上下文
审核操作和审核意见
```

普通帖子详情不应该承担这些责任，否则会出现：

- 审核员没有完整证据；
- 审核动作和阅读动作混在一起；
- 举报内容无法定位具体违规段落；
- 管理员只能在列表上直接“通过/拒绝”，无法充分复核。

### 建议

新增独立页面：

```text
/admin/content/review/:id
```

或：

```text
/admin/posts/:id/review
```

页面采用“证据 + 决策”布局：

```text
左侧 65%：帖子内容预览
右侧 35%：审核证据、状态和操作面板
```

右侧操作面板包含：

- 通过并发布；
- 拒绝并填写原因；
- 要求补充资料；
- 暂存待处理；
- 隐藏/下架；
- 查看作者历史；
- 查看相似帖子；
- 查看相关举报。

审核页不能直接使用普通用户互动按钮，例如点赞、收藏和“不感兴趣”。管理员是在做治理决策，不是在浏览社区。

---

### P0-3 举报中心只有快照，没有审查上下文

当前举报列表主要展示：

- 被举报对象类型；
- `target_snapshot`；
- 举报原因；
- 举报人；
- 补充说明。

这对简单演示够用，但正式审核不够。

举报管理员应该能看到：

- 被举报帖子完整内容；
- 被举报评论所在的上下文；
- 评论前后若干条评论；
- 举报人提交的理由和证据；
- 被举报作者信息；
- 作者近期违规次数；
- 是否已有其他用户举报；
- 当前帖子公开状态；
- 是否已经被其他管理员处理；
- 处理历史和审计记录。

### 建议

新增举报详情页：

```text
/admin/reports/:id
```

处理操作必须明确区分：

```text
举报成立：隐藏帖子 / 删除评论 / 限制作者
举报驳回：保留内容并记录原因
需要补充：暂不结案，等待证据
误报：标记为恶意举报，进入举报人风险统计
```

举报不能只提供“成立”和“驳回”两个粗粒度按钮。至少要提供处理原因和管理员备注，避免后续无法解释为什么采取措施。

---

### P0-4 城市专题和攻略内容没有后台运营闭环

当前城市专题服务主要是聚合：

- 推荐景点；
- 已发布用户帖子；
- 景点和帖子数量。

当前 RAG 攻略由：

```text
classpath*:guides/*.md
```

启动时加载并切分。

这意味着现在的本地攻略更接近“随程序发布的静态知识文件”，不是可运营内容。管理员无法在系统里：

- 新增城市攻略；
- 编辑攻略；
- 审核攻略；
- 下线过时攻略；
- 标记攻略来源；
- 调整攻略优先级；
- 查看攻略命中了哪些景点；
- 让攻略变更后重新构建 RAG 索引；
- 回滚错误编辑。

所以你的判断“这些攻略难道纯写死吗”是准确的。

这里还需要明确一个关键问题：管理员后台攻略与 RAG 攻略文档不能各自维护两份线上内容，否则很快会出现“后台显示一版、RAG 检索另一版”的数据分裂。

推荐的原则是：

```text
数据库中的已发布攻略版本 = 线上唯一主数据
RAG 索引 = 已发布攻略版本的派生数据
Markdown 文件 = 初始化导入、人工导出、代码评审和备份载体
```

不要把“数据库”和“resources/guides/*.md”设计成两个可以独立编辑的主数据源，也不建议让生产环境运行时反向修改 classpath 里的 Markdown 文件。打包后的 JAR、Docker 镜像和多实例环境都不适合这样做。

### 建议

攻略内容应该从静态文件逐步迁移到数据库管理，静态 Markdown 只保留为初始化种子或离线导入格式。

推荐新增内容实体：

```text
city_guide
- id
- city
- title
- summary
- content_markdown
- cover_image
- source_type
- source_name
- source_url
- author
- status
- quality_score
- version
- submitted_by
- reviewed_by
- published_at
- created_at
- updated_at
```

状态建议：

```text
DRAFT
PENDING_REVIEW
PUBLISHED
REJECTED
HIDDEN
ARCHIVED
```

管理员还需要看到攻略的结构化解析结果：

```text
攻略命中的景点
攻略推导的标签
攻略覆盖的城市
攻略切分出的 RAG 片段
向量索引状态
最后索引时间
```

---

## 三、管理员端整体信息架构

### 3.1 管理后台首页 Dashboard

管理员登录后首先进入 `/admin`，不是普通用户首页。

首页目标是回答：

```text
现在有什么需要我处理？
内容质量怎么样？
哪些城市和推荐流需要运营？
最近发生了什么风险事件？
```

建议模块：

#### 今日待办

- 待审核帖子数量；
- 待处理举报数量；
- 待审核攻略数量；
- 待处理城市内容变更；
- 低质内容数量；
- 失败的 RAG 索引任务。

#### 内容概览

- 今日新增帖子；
- 今日发布帖子；
- 今日隐藏内容；
- 近 7 天举报量；
- 举报成立率；
- 平均审核处理时长。

#### 内容质量

- 低质量帖子比例；
- 自动规则拦截数量；
- 重复内容比例；
- 城市攻略覆盖数量；
- POI_ONLY 景点比例；
- 过期攻略数量。

#### 系统状态

- 数据库连接；
- Redis 状态；
- RAG 索引状态；
- 高德 API 状态；
- LLM API 状态；
- 最近失败任务。

#### 最近操作

展示：

```text
谁在什么时候对哪个内容做了什么
```

例如：

```text
管理员 admin 3 分钟前拒绝了帖子《三亚三日游》
管理员 admin 8 分钟前下架了帖子《虚假门票信息》
系统 12 分钟前完成了成都攻略索引
```

---

### 3.2 内容审核中心

路径：

```text
/admin/content/review
```

分为三个队列：

```text
待审核
复审中
已处理
```

每条内容显示：

- 内容类型：帖子 / 攻略 / 景点描述 / 评论；
- 标题和摘要；
- 作者或来源；
- 所属城市；
- 自动风险等级；
- 质量分；
- 举报数；
- 提交时间；
- 当前处理人。

建议风险等级：

```text
高风险：敏感内容、广告导流、疑似诈骗、明显虚假信息
中风险：低质、重复、信息不完整、图片疑似不相关
低风险：自动规则通过，等待人工抽查
```

排序建议：

1. 高风险优先；
2. 举报数多优先；
3. 提交时间早优先；
4. 高曝光内容优先；
5. 普通内容按时间排序。

---

### 3.3 帖子治理

路径：

```text
/admin/posts
```

功能包括：

- 搜索帖子；
- 按城市筛选；
- 按作者筛选；
- 按状态筛选；
- 按质量分筛选；
- 按举报数量筛选；
- 批量隐藏；
- 恢复公开；
- 查看审核记录；
- 查看内容版本；
- 查看作者历史违规。

必须增加批量操作前确认，避免误操作大范围下架。

已发布帖子被作者修改后重新审核的机制应保留：

```text
公开版本继续可见
编辑版本进入待审
审核通过后切换公开版本
```

长期应增加 `post_revision`，不要只在同一条记录上覆盖正文。

---

### 3.4 举报中心

路径：

```text
/admin/reports
```

列表筛选：

- 待处理；
- 已成立；
- 已驳回；
- 需要补充；
- 帖子举报；
- 评论举报；
- 举报原因；
- 时间范围；
- 处理人。

详情页布局：

```text
左侧：被举报内容和上下文
右侧：举报证据、作者信息、历史记录、处理动作
```

处理动作：

- 维持公开；
- 隐藏帖子；
- 删除评论；
- 拒绝发布；
- 限制作者发帖；
- 暂停作者账号；
- 标记恶意举报；
- 记录管理员备注。

注意：账号处罚必须单独设计权限和确认机制，不能把“举报成立”直接等同于“封禁用户”。

---

## 四、城市专题与攻略运营中心

这是当前最需要补齐的产品能力之一。

### 4.1 城市管理

路径：

```text
/admin/cities
```

城市列表应显示：

- 城市名称；
- 是否有本地攻略；
- 攻略数量；
- 已发布景点数量；
- POI_ONLY 比例；
- 最近同步时间；
- 最近攻略更新时间；
- RAG 索引状态；
- 是否在推荐城市列表中；
- 是否启用城市专题。

城市操作：

- 新增城市；
- 编辑城市名称、别名和封面；
- 上线/下线城市专题；
- 手动触发景点同步；
- 手动触发攻略索引；
- 查看城市内容质量；
- 导出城市内容报告。

### 4.2 攻略管理

路径：

```text
/admin/guides
```

攻略来源至少分为：

```text
CURATED：管理员维护的官方/编辑精选攻略
COMMUNITY：用户发布后被选为城市精选的帖子
IMPORTED：导入的外部资料
SYSTEM：系统初始化 Markdown 攻略
```

后台列表展示：

- 城市；
- 标题；
- 来源类型；
- 来源链接；
- 作者/维护人；
- 状态；
- 质量分；
- 覆盖景点数；
- 标签数量；
- RAG 片段数；
- 索引状态；
- 更新时间。

后台操作：

- 新建攻略；
- Markdown 编辑；
- 预览；
- 保存草稿；
- 提交审核；
- 发布；
- 下线；
- 归档；
- 复制为新版本；
- 回滚上一版本；
- 重新构建索引。

### 4.3 攻略编辑器

建议采用左右分栏：

```text
左侧：Markdown 编辑器
右侧：实时预览
底部：结构化检查结果
```

结构化检查包括：

- 是否有城市标题；
- 是否包含有效景点；
- 是否包含明显过期的门票和营业时间；
- 是否包含外部来源；
- 是否存在重复段落；
- 是否命中过多不确定表达；
- 是否能解析出景点卡片；
- 是否能生成有效标签。

管理员保存时不应直接影响线上 RAG。必须采用版本状态：

```text
编辑草稿 -> 预览 -> 提交审核 -> 发布 -> 构建索引 -> 索引成功后生效
```

索引失败时：

- 保留旧版本继续服务；
- 后台显示失败原因；
- 不允许新内容悄悄替换线上内容；
- 支持手动重试。

### 4.4 攻略与景点关联

攻略编辑页应显示自动解析结果：

```text
本篇攻略识别到 8 个景点
其中 6 个已在 spot 表中
2 个未匹配
```

管理员可以：

- 确认景点匹配；
- 修改景点关联；
- 创建缺失景点；
- 标记同名景点；
- 取消错误关联。

这能直接改善景点详情页的简介、标签和推荐理由质量。

---

## 五、景点数据运营中心

路径：

```text
/admin/spots
```

当前景点主档主要由高德同步和 RAG 回填，管理员还需要看到和修正数据质量。

列表字段：

- 景点名称；
- 城市；
- POI ID；
- 数据质量：POI_ONLY / GUIDE_MATCHED / VERIFIED；
- 简介来源；
- 地址和坐标；
- 图片状态；
- 最近同步时间；
- 是否被用户收藏；
- 是否被行程采用；
- 是否被标记异常。

操作：

- 编辑规范名称；
- 修正城市和地址；
- 合并重复景点；
- 标记非景点；
- 下线错误 POI；
- 标记已关闭/过期；
- 手动触发重新同步；
- 重新匹配攻略；
- 查看数据来源和变更记录。

注意：管理员手工修正不能被下一次高德同步无条件覆盖。建议增加字段：

```text
manual_override
manual_override_fields
last_verified_by
last_verified_at
```

同步时只更新未被人工锁定的字段。

---

## 六、推荐与个性化运营中心

当前已经有 A/B 实验和推荐流监控，这是很好的基础，但管理员端还应该能解释和干预推荐数据。

路径：

```text
/admin/recommendations
```

建议模块：

### 推荐流概览

- 推荐景点曝光；
- 个性化命中率；
- 收藏率；
- 不感兴趣率；
- 各城市推荐量；
- POI_ONLY 占比；
- 攻略命中率；
- 推荐失败率。

### 城市推荐质量

对每个城市显示：

- 可推荐景点数量；
- 有攻略景点数量；
- 无攻略景点数量；
- 平均质量分；
- 用户收藏率；
- 用户负反馈率；
- 最近同步时间；
- 最近 RAG 更新时间。

### 推荐人工干预

首版不建议让管理员直接手填最终分数。可以提供低风险运营能力：

- 景点置顶；
- 景点降权；
- 推荐黑名单；
- 城市精选标记；
- 指定攻略优先；
- 设置生效时间和失效时间；
- 填写操作原因。

所有干预必须记录审计日志，并显示“人工干预”标识，避免运营规则和算法分数混在一起。

---

## 七、用户与账号治理

路径：

```text
/admin/users
```

当前用户列表主要是查看用户，不足以支撑治理。

建议增加：

- 用户搜索；
- 注册时间筛选；
- 发帖数、举报数、违规次数；
- 当前账号状态；
- 管理员角色状态；
- 最近登录时间；
- 内容质量概况；
- 查看用户公开内容；
- 限制发帖；
- 暂停评论；
- 暂停账号；
- 恢复账号。

角色建议不要只保留 USER/ADMIN，长期可拆分：

```text
USER：普通用户
CONTENT_REVIEWER：内容审核员
CITY_EDITOR：城市/攻略编辑
RECOMMENDATION_OPERATOR：推荐运营
SUPER_ADMIN：系统管理员
```

权限采用“角色 + 权限点”，不要所有后台人员都拥有全部能力。

例如：

```text
post.review
report.handle
guide.edit
guide.publish
spot.edit
recommendation.override
user.suspend
audit.read
system.settings
```

高风险操作需要二次确认，超级管理员权限需要单独保护。

---

## 八、管理员端视觉和交互设计

### 8.1 与普通用户端彻底区分

普通用户端：

```text
旅行灵感、图片、卡片、路线、社区内容、轻松阅读
```

管理员端：

```text
密集信息、队列、状态、风险、证据、批量处理、审计
```

管理员端不应继续使用普通用户的：

- 旅行推荐卡；
- 加入行程按钮；
- 点赞收藏按钮；
- 普通社区帖子阅读入口；
- 个人画像模块；
- 面向游客的营销文案。

### 8.2 推荐后台布局

```text
┌──────────────┬──────────────────────────────────────┐
│ 管理后台 Logo │ 顶部：面包屑 / 搜索 / 通知 / 管理员菜单 │
├──────────────┼──────────────────────────────────────┤
│ 总览           │                                      │
│ 内容审核       │ 当前页面标题                          │
│ 举报中心       │ KPI / 筛选 / 队列 / 证据              │
│ 帖子治理       │                                      │
│ 攻略内容       │                                      │
│ 城市专题       │                                      │
│ 景点数据       │                                      │
│ 推荐运营       │                                      │
│ 用户治理       │                                      │
│ 审计日志       │                                      │
└──────────────┴──────────────────────────────────────┘
```

### 8.3 审核页面设计原则

- 状态颜色明确，但不能只依赖颜色；
- 通过、拒绝、下架、封禁等危险操作必须区分颜色和确认步骤；
- 长文本支持折叠和定位；
- 规则命中内容高亮；
- 被举报段落直接定位；
- 操作后显示审计结果和下一步状态；
- 支持键盘快捷操作，但不能误触发危险动作；
- 列表支持批量选择，但批量操作必须二次确认。

---

## 九、数据和后端改造建议

### 9.1 新增攻略实体和版本表

推荐至少增加：

```text
city_guide
city_guide_revision
city_guide_spot
city_guide_tag
rag_index_task
```

职责：

- `city_guide`：攻略当前状态和当前发布版本；
- `city_guide_revision`：每次编辑的版本；
- `city_guide_spot`：攻略与景点关联；
- `city_guide_tag`：攻略标签；
- `rag_index_task`：索引任务、状态、错误和重试。

建议再增加以下字段或表约束：

```text
city_guide.current_revision_id
city_guide.published_revision_id
city_guide.source_file
city_guide.source_hash
city_guide.rag_status
city_guide.rag_indexed_revision
city_guide_revision.content_hash
city_guide_revision.editor_id
city_guide_revision.change_summary
```

其中：

- `published_revision_id` 指向当前线上可见版本；
- `current_revision_id` 指向管理员正在编辑的版本；
- `content_hash` 用于判断内容是否真的变化，避免无变化重复索引；
- `rag_indexed_revision` 表示最后成功进入 RAG 的版本；
- `rag_status` 至少包括 `NOT_INDEXED`、`INDEXING`、`READY`、`FAILED`；
- `source_file` 只记录该攻略最初从哪个 Markdown 文件导入，不代表线上继续读取该文件。

### 9.2 RAG 与管理员攻略的同步关系

当前 `RagService` 在启动时扫描 `classpath*:guides/*.md`，切分后建立内存检索和向量缓存。这种方式适合初始化数据，但不适合管理员在线运营。改造后必须把“内容保存”和“RAG 生效”拆成两个阶段。

#### 推荐的单向同步链路

```text
resources/guides/*.md（初始化种子）
  │ 启动/部署时一次性导入
  ▼
city_guide + city_guide_revision（数据库主数据）
  │ 管理员编辑、审核、发布
  ▼
published_revision_id
  │ 创建并执行 RAG 索引任务
  ▼
guide_chunk / guide_embedding / Chroma 集合
  │ 索引成功后切换版本
  ▼
RagService 只检索当前已发布且索引成功的版本
```

这不是“数据库和 Markdown 双向同步”，而是：

```text
Markdown -> 数据库：初始化导入
数据库 -> RAG：发布后派生索引
数据库 -> Markdown：管理员主动导出备份，可选
```

数据库不应自动回写生产 JAR 内的 Markdown 文件。需要代码仓库版本化时，提供“导出 Markdown”功能，由管理员或开发人员审核后提交 Git，而不是由线上服务直接改 Git 工作区。

#### 管理员编辑和发布流程

```text
1. 管理员打开攻略
2. 编辑数据库中的草稿版本
3. 保存 DRAFT
4. 系统做 Markdown 结构检查、敏感内容检查、景点关联检查
5. 提交 PENDING_REVIEW
6. 审核员通过，生成新的 published_revision
7. 创建 rag_index_task
8. 切分文本、生成 embedding、写入向量库
9. 索引全部成功后，将该 revision 标记 READY
10. 原子切换 city_guide.published_revision_id
11. 清理旧版本缓存并刷新 RagService
12. 城市专题、景点简介和行程生成开始使用新版本
```

发布和索引不能反过来。不能先把新攻略标记为线上版本，再慢慢等待 embedding，否则用户可能看到新标题，却检索到旧内容或空内容。

#### 索引失败时的行为

索引失败时必须保持旧版本继续服务：

```text
新版本：INDEX_FAILED
旧版本：继续 PUBLISHED/READY
后台：显示失败原因、失败时间和重试按钮
```

不能因为一次 embedding、Chroma 或数据库写入失败，就让整个城市的 RAG 变成空结果。管理员可以修复内容后重试，也可以放弃该版本并回滚草稿。

#### 是否需要实时同步

不建议每次管理员点击“保存草稿”都触发 RAG。推荐两种触发点：

- **提交审核**：只做静态检查，不生成 embedding；
- **审核发布**：创建唯一索引任务，异步执行；
- **管理员手动重建**：用于修复索引或更换 embedding 模型。

这样既避免草稿污染线上推荐，也避免管理员连续输入时反复消耗 embedding 配额。

#### 版本切换与缓存失效

RAG 查询缓存必须带攻略版本号或内容版本标识：

```text
rag:{city}:{published_revision_id}:{query_hash}
```

发布新版本成功后：

1. 旧城市 RAG 查询缓存失效；
2. 旧城市内存向量索引移除或标记过期；
3. 新版本索引状态确认 READY；
4. 新请求只读取新版本；
5. 正在执行的旧任务不能覆盖新版本。

`rag_index_task` 必须带 `revision_id`，不能只按城市执行，否则旧任务晚到时可能把旧内容重新写回线上。

#### 现有 Markdown 的迁移方式

第一阶段不要立刻删除现有 `resources/guides/*.md`，建议做一次受控导入：

```text
扫描 Markdown
 -> 提取城市、标题、正文和来源文件名
 -> 计算 content_hash
 -> 写入 city_guide / city_guide_revision
 -> 标记为 SYSTEM 来源
 -> 构建初始 RAG 索引
 -> 校验检索结果
```

导入必须幂等：同一个 `source_file + content_hash` 不重复创建版本。导入完成后：

- 新攻略由管理员后台进入数据库；
- 原 Markdown 作为初始化备份保留；
- 城市专题和 RAG 不再直接依赖 classpath 文件；
- 后续 Markdown 变更需要通过“重新导入”或管理员导入接口进入数据库，不能悄悄覆盖线上版本。

如果未来仍希望通过 Git 管理 Markdown，可以增加明确的“导入流程”：

```text
Git Markdown 更新
 -> CI/管理员点击导入
 -> 生成数据库草稿版本
 -> 审核
 -> 发布并索引
```

这叫“文件导入数据库”，不是两边实时双向同步。必须明确冲突处理：数据库存在未发布编辑时，禁止无提示覆盖，要求管理员选择“保留数据库草稿 / 以文件创建新版本 / 放弃文件变更”。

### 9.3 内容来源和可信度

管理员后台要显示来源：

```text
管理员编辑
官方来源
用户投稿
高德 POI
外部导入
模型生成草稿
```

模型生成的内容不能直接进入公开攻略，必须经过人工确认或明确标记为“待核实”。

### 9.4 所有运营操作写审计

必须审计：

- 谁编辑了攻略；
- 谁发布/下线了攻略；
- 谁修改了景点；
- 谁处理了举报；
- 谁隐藏/恢复帖子；
- 谁调整了推荐权重；
- 谁暂停/恢复账号；
- 谁触发了 RAG 重建。

审计详情要包含：

```text
操作者、时间、目标对象、旧值、新值、操作原因、请求追踪 ID
```

---

## 十、路由和权限建议

### 前端路由

```text
/admin
/admin/content/review
/admin/content/review/:id
/admin/reports
/admin/reports/:id
/admin/posts
/admin/posts/:id
/admin/guides
/admin/guides/create
/admin/guides/:id/edit
/admin/cities
/admin/cities/:name
/admin/spots
/admin/spots/:id
/admin/recommendations
/admin/users
/admin/users/:id
/admin/audit
```

### 路由守卫

前端可以做体验级守卫，但权限必须由后端再次校验：

```ts
meta: { requiresAdmin: true }
```

路由守卫行为：

- 未登录：跳登录页；
- 已登录非管理员：跳普通用户首页并提示无权限；
- 管理员访问普通用户端：允许通过“返回用户端”进入；
- 管理员进入 `/admin`：使用 `AdminLayout`，不加载 `AppLayout`。

### 后端接口分组

建议统一前缀：

```text
/api/admin/dashboard
/api/admin/review/*
/api/admin/reports/*
/api/admin/guides/*
/api/admin/cities/*
/api/admin/spots/*
/api/admin/recommendations/*
/api/admin/users/*
/api/admin/audit/*
```

现有 `/community/moderation/**` 和 `/admin/**` 可以保留兼容，但新功能应逐步统一到 `/api/admin/**`。

---

## 十一、分阶段实施计划

### 第一阶段：先把后台从普通用户端剥离

1. 新增 `AdminLayout.vue`；
2. 新增 `/admin` 路由和 `requiresAdmin` 守卫；
3. `/moderation` 重定向到 `/admin/content/review`；
4. 管理员登录后进入管理首页；
5. 普通用户端不显示管理导航；
6. 保留“返回用户端”入口；
7. 把现有审核 tabs 拆成独立管理模块。

### 第二阶段：完善审核和举报证据页

1. 新增管理员帖子审核详情页；
2. 新增举报详情页；
3. 显示规则命中、质量分、作者历史和关联举报；
4. 支持审核备注和处理原因；
5. 增加处理状态和审计结果；
6. 不再从管理页面跳普通用户帖子详情。

### 第三阶段：攻略和城市运营

1. 增加 `city_guide` 草稿和发布状态；
2. 攻略编辑器支持 Markdown 预览；
3. 攻略与景点关联；
4. 城市专题管理；
5. 攻略发布后创建 RAG 索引任务；
6. 展示索引状态和失败重试；
7. 静态 Markdown 作为初始化数据导入数据库。

### 第四阶段：景点和推荐运营

1. 景点数据治理列表；
2. 人工修正和字段锁定；
3. 重复景点合并；
4. 城市推荐质量看板；
5. 推荐置顶、降权、黑名单；
6. A/B 实验与人工规则统一展示；
7. 运营动作审计。

### 第五阶段：角色和权限细化

1. USER；
2. CONTENT_REVIEWER；
3. CITY_EDITOR；
4. RECOMMENDATION_OPERATOR；
5. SUPER_ADMIN；
6. 权限点控制和高风险操作二次确认。

---

## 十二、验收标准

### 管理员体验

- 管理员登录后默认进入 `/admin`，而不是普通用户首页；
- 管理端拥有独立布局、导航和视觉层级；
- 可以一键返回普通用户端；
- 普通用户无法看到或访问管理页面；
- 管理员端不出现普通用户的收藏、点赞和加入行程操作。

### 审核体验

- 待审核帖子点击进入管理员审核详情页；
- 不再直接打开普通用户帖子详情；
- 审核员能看到正文、作者、规则命中、质量分和历史；
- 拒绝必须填写原因；
- 下架、恢复、通过、拒绝都有审计记录。

### 举报体验

- 举报列表能看到举报原因和基本快照；
- 举报详情能看到完整被举报内容和上下文；
- 评论举报能看到评论所在帖子；
- 处理时能填写管理员备注；
- 举报处理结果有明确状态和操作记录；
- 举报成立不自动等同于封禁账号。

### 攻略和城市运营

- 管理员可以新增、编辑、审核、发布和下线攻略；
- 城市专题内容不再只能依赖静态 Markdown；
- 数据库中的已发布攻略版本是城市专题和 RAG 的同一内容来源；
- 现有 Markdown 能幂等导入数据库，并保留来源文件和内容哈希；
- 攻略发布后能按 revision 创建 RAG 索引任务；
- 新版本索引成功后才切换线上版本，旧任务不能覆盖新版本；
- 索引失败不会破坏旧版本线上服务；
- 发布后相关 RAG 查询缓存和旧向量索引会失效；
- 管理员可以导出 Markdown 作为备份，但线上服务不直接修改 JAR 内文件；
- 管理员能看到攻略与景点的关联结果；
- 城市、景点、攻略的手工修改可追溯。

### 工程质量

- 后端所有管理员接口继续进行服务端权限校验；
- 前端路由守卫不能替代后端鉴权；
- 新增后台页面使用动态路由加载；
- 后端测试覆盖权限、状态机、发布、下线和索引失败回滚；
- 前端构建通过；
- 不改变普通用户已有登录、规划、推荐和社区流程。

---

## 十三、可以直接交给 Workbuddy 的执行提示词

请基于 `管理员后台与内容运营中心设计方案.md`，把当前管理员模块从普通用户工作台中独立出来。

必须优先完成：

1. 新增独立 `AdminLayout.vue`，管理员默认进入 `/admin`；
2. 增加 `requiresAdmin` 路由守卫，并保留后端服务端权限校验；
3. 将 `/moderation` 兼容重定向到新的管理审核页；
4. 新增管理员专用帖子审核详情页，禁止审核列表点击后进入普通用户 `PostDetail.vue`；
5. 新增举报详情页，展示被举报帖子/评论完整内容、上下文、举报历史和处理动作；
6. 管理员界面不能出现普通用户点赞、收藏、加入行程等操作；
7. 新增城市和攻略运营模块设计，至少先完成数据库实体、列表、详情、草稿、发布状态和接口骨架；
8. 明确数据库中的已发布攻略版本是唯一线上主数据，RAG 向量/分片是派生数据，Markdown 只作为初始化导入、导出备份和 Git 评审载体；
9. 不要让数据库和 `resources/guides/*.md` 成为两个可以独立编辑的线上主数据源，也不要让生产服务直接回写 JAR 内 Markdown；
10. 静态 Markdown 必须提供幂等导入流程，使用 `source_file + content_hash` 防止重复导入，并处理数据库草稿与文件变更冲突；
11. 攻略发布后按 `revision_id` 创建 RAG 索引任务，索引成功后再原子切换 `published_revision_id`；
12. 索引失败时旧版本继续服务，新版本标记失败并支持重试，旧任务不能覆盖新版本；
13. 发布新版本后清理城市 RAG 缓存、内存向量索引和 Chroma 旧集合/旧版本数据；
14. 城市专题、攻略、景点数据修改必须有状态、版本和审计记录；
15. 管理后台视觉上使用独立布局，普通用户端和管理员端有明显区分；
16. 完成后运行后端测试和 `npm run build`，汇报改动文件、接口、迁移脚本、导入策略、索引失败回滚验证结果。

不要做以下事情：

- 不要只在现有 `Moderation.vue` 里继续增加 tabs；
- 不要让管理员审核时复用普通用户帖子详情页；
- 不要把举报列表的快照当成完整审查能力；
- 不要让前端隐藏按钮成为唯一权限控制；
- 不要让管理员直接修改线上 RAG 内容而没有草稿、审核、版本和回滚；
- 不要做无版本号、无任务状态、无幂等保护的“保存后直接重建全部向量”；
- 不要让旧的异步索引任务在新版本发布后覆盖当前线上版本；
- 不要一次性实现复杂封禁系统而没有操作审计和二次确认。

---

## 十四、最终建议

这个系统现在最需要的不是继续往普通用户端叠加更多功能，而是建立第二个真正的产品面：

```text
普通用户端负责使用旅行内容
管理员端负责生产、审核、维护和解释旅行内容
```

城市专题不是静态页面，攻略不是永远写死的 Markdown，景点也不是只能被高德同步覆盖的数据。它们应该成为可管理、可审核、可版本化、可回滚、可追踪的内容资产。

后台改造的核心不是“把按钮换个位置”，而是建立完整的内容运营闭环：

```text
内容创建
  -> 自动检查
  -> 人工审核
  -> 发布
  -> RAG 索引
  -> 城市专题展示
  -> 推荐流使用
  -> 用户反馈
  -> 内容质量复盘
  -> 版本更新或下线
```

当这个闭环建立起来后，系统才真正从“功能比较丰富的旅行应用”进化为“有内容生产和运营能力的旅行平台”。
