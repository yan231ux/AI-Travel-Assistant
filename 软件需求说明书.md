# AI旅游助手 软件需求说明书（SRS）

| 项目名称 | AI旅游助手（AI 旅行规划系统） |
|---|---|
| 文档版本 | V1.0 |
| 编写日期 | 2026-08-29 |
| 文档类型 | 软件需求说明书（Software Requirements Specification） |
| 对标 | 本科毕业设计答辩评审标准 |

---

## 1. 引言

### 1.1 编写目的

本文档定义 **AI旅游助手**（以下简称"本系统"）的完整软件需求，包括功能需求、非功能需求、数据需求与接口需求，作为系统设计、开发实现、测试验收与毕业设计答辩评审的依据。

### 1.2 项目背景

旅行规划涉及目的地、天气、景点、交通、住宿、预算等多维信息，人工规划费时费力。现有通用大模型虽能生成行程，但存在**内容不可信**（地址张冠李戴、预算编造）、**知识时效与私有性**（依赖模型内置知识）等问题。本系统面向个人游客，以 **AI 智能体 + 本地知识库（RAG）** 为核心，提供可信、可追溯、可个性化的行程自动生成服务。

### 1.3 系统目标

1. 用户输入目的地、日期、人数、预算、偏好等信息，系统自动生成结构化行程；
2. 行程内容**事实可信**：景点地址/简介/门票来自人工整理的攻略或高德真实数据，杜绝模型幻觉；
3. 用户特殊需求（点名景点）**硬保证**满足，无法满足时明确告知原因；
4. 具备**长期记忆**：基于用户历史行程实现个性化推荐；
5. 生成过程**可视化**：实时展示 AI 思考轨迹，性能可接受（同参数秒回）。

### 1.4 术语定义

| 术语 | 说明 |
|---|---|
| ReAct | Reasoning + Acting，智能体"思考→行动→观察"多轮决策循环 |
| RAG | Retrieval-Augmented Generation，检索增强生成 |
| RRF | Reciprocal Rank Fusion，倒数排名融合算法 |
| BM25 | 关键词稀疏检索算法 |
| Grounding | 事实锚定：用可信数据源强制约束模型输出 |
| SSE | Server-Sent Events，服务器推送事件（流式响应） |
| POI | Point of Interest，兴趣点（高德地图地点数据） |
| 攻略卡片 | 从攻略 Markdown 解析出的结构化景点条目（位置/门票/时长/简介/注意） |

### 1.5 参考资料

- 项目答辩底稿（.workbuddy/memory/2026-08-20.md 等）
- 技术难点与解决记录（TROUBLESHOOTING.md）
- 高德地图开放平台 API 文档、DashScope 百炼 API 文档、Open-Meteo API 文档

---

## 2. 总体描述

### 2.1 产品定位

**B/S 架构的 AI 驱动旅行规划系统**。面向个人游客的轻量级 Web 应用，核心价值 = **可信的内容 + 可见的过程 + 会记忆的智能体**。

### 2.2 用户角色

| 角色 | 说明 | 主要需求 |
|---|---|---|
| 注册用户 | 登录后使用完整功能 | 生成/保存/查看/删除/导出行程、个性化推荐 |
| 匿名访问者 | 未登录 | 仅可访问登录/注册页（受保护资源不可达） |
| 系统管理员（答辩维度） | 维护攻略知识库 | 通过放置 Markdown 文件扩展城市攻略（目录驱动，无需改代码） |

### 2.3 运行环境

| 项 | 要求 |
|---|---|
| 服务端 | Windows / Linux，JDK 21，Spring Boot 3.3.5 |
| 数据库 | MySQL 8.0（Docker Compose 编排，端口 3310） |
| 缓存 | Redis 7（可选，端口 6379，不可用自动降级） |
| 向量库 | ChromaDB（可选加速层，不可达自动降级内存余弦） |
| 前端 | Chrome/Edge 等现代浏览器（Vue3 + Vite + Ant Design Vue） |
| 外部依赖 | DashScope 大模型 API、高德地图 API、Open-Meteo 天气、Bing 搜索（均有 Key 环境变量配置） |

### 2.4 技术架构

```
┌─────────────────────────────────────────────────────────┐
│ 前端 Vue3 + Vite + AntDV + TS + ECharts + 高德JS API      │
│ 表单(日历选择器) / Agent轨迹动画 / 行程结果 / 历史列表 / 地图 │
└──────────────────────────┬──────────────────────────────┘
                           │ HTTP / SSE（Bearer JWT）
┌──────────────────────────▼──────────────────────────────┐
│ 后端 Spring Boot 3.3.5                                   │
│ controller(门面) → agent(ReAct智能体) → service(业务)      │
│   → client(外部API) / repository(SQL) / security(JWT)     │
│ 六层清晰分层，职责单一                                     │
└──────────────────────────────────────────────────────────┘
```

**核心技术栈**：Spring Boot 3.3.5 + MyBatis-Plus + MySQL 8 + Redis + Docker Compose + DashScope LLM + 高德 API + Vue3/Vite/AntDV + ECharts。

### 2.5 功能结构总览

| 模块 | 核心功能 |
|---|---|
| M1 用户管理 | 注册、登录（JWT+BCrypt）、请求鉴权 |
| M2 城市校验 | 白名单/别名/编辑距离纠错/高德行政区划兜底，四道防线 |
| M3 行程生成（核心） | ReAct 智能体、工具编排、SSE 流式、点名景点保障 |
| M4 RAG 知识库 | 混合检索（向量+BM25+RRF）、目录驱动、攻略卡片 |
| M5 数据可信度 | 校验层：去重/天气PlanB/预算修正/地址绑定/卡片回写/来源标注 |
| M6 个性化 | 长期记忆画像构建与注入 |
| M7 行程管理 | 列表/详情/保存/删除 |
| M8 导出 | Markdown / PDF |
| M9 前端交互 | 表单、轨迹动画回放、地图、结果可视化 |

---

## 3. 功能需求

> 优先级定义：**P0**=核心必备（缺失即不可用）；**P1**=重要（影响体验/可信度）；**P2**=增强（锦上添花）。
> 每条需求含验收要点，可直接转为测试用例与答辩演示脚本。

### 3.1 用户管理模块（M1）

| 编号 | 需求 | 优先级 | 验收要点 |
|---|---|---|---|
| FR-1.1 | **注册**：POST /auth/register，用户名唯一、密码 BCrypt 加密存储 | P0 | 重复用户名返回明确错误；库中无明文密码 |
| FR-1.2 | **登录**：POST /auth/login，校验通过签发 JWT（HS256，7 天有效） | P0 | 正确凭证返回 token；错误凭证返回 401 |
| FR-1.3 | **鉴权拦截**：自定义 HandlerInterceptor + ThreadLocal 传递用户，非白名单接口未带有效 token 一律 401 | P0 | 未登录访问行程接口返回 401；登录后携带 Bearer token 可访问 |
| FR-1.4 | 用户数据隔离：行程增删改查均按服务端解析的 userId 过滤，请求体中的 user_id 一律忽略（防伪造归属） | P0 | A 用户无法查看/删除 B 用户的行程 |

### 3.2 城市校验模块（M2）

| 编号 | 需求 | 优先级 | 验收要点 |
|---|---|---|---|
| FR-2.1 | **四道防线**：①70+ 城市白名单+别名表 ②剥"市/省"后缀 ③Levenshtein 编辑距离纠错提示 ④高德行政区划级兜底；仅允许市/区/县等行政区划，拒绝 POI/道路/自然地名 | P0 | "成都""成都市""chengdu"均可识别；"火星市""随便市"直接 400 拒绝 |
| FR-2.2 | 校验失败在 Controller 前置抛 400，**不启动 Agent、不消耗任何模型 token** | P0 | 假城市请求后端无 LLM 调用日志 |

### 3.3 行程生成模块（M3，核心）

| 编号 | 需求 | 优先级 | 验收要点 |
|---|---|---|---|
| FR-3.1 | **ReAct 智能体**：THINK→ACT→OBSERVE 循环 ≤3 轮；LLM 基于工具目录动态决策调用哪些工具（web_search / weather_forecast / amap_poi / rag_guide），非硬编码流水线 | P0 | 轨迹中可见"计划→执行→反思→生成"四阶段；首轮计划由 LLM 生成，解析失败有规则兜底 |
| FR-3.2 | **工具并行执行**：多工具并行、统一超时（8s）；单个工具失败记录 gap 不中断整体 | P0 | 任一工具异常不影响生成流程 |
| FR-3.3 | **数据充分性规则短路**：有 POI 且有（搜索或 RAG）且有天气 视为足够；规则判定足够时跳过 LLM 反思调用 | P0 | Step3 不再等待多余 LLM 往返 |
| FR-3.4 | **SSE 流式推送**：POST /trip/generate-stream，事件类型 progress/step/itinerary/done/error；客户端断开提前退出释放线程 | P0 | 前端逐阶段动画展示；断开后后端日志显示提前收尾 |
| FR-3.5 | **生成超时保护**：主生成 LLM 90s 超时、JSON 修正 20s，超时抛业务异常经 SSE error 反馈，前端不无限等待 | P0 | 慢响应场景前端出现错误提示而非白屏等待 |
| FR-3.6 | **点名景点保障**：从特殊需求提取点名景点（攻略卡片反向匹配 + 高德定向查询）；提示词硬规则"必须进行程"；生成后校验，未安排则结果中明确说明原因 | P0 | 额外要求"想去上海野生动物园"（攻略无）仍进入行程且地址真实；无法满足时出现"未能安排"说明 |
| FR-3.7 | 输入校验：目的地非空、日期区间合法（结束≥开始、≤31 天）、人数≥1、预算≥0 | P0 | 非法输入返回 400 明确提示 |

### 3.4 RAG 知识库模块（M4）

| 编号 | 需求 | 优先级 | 验收要点 |
|---|---|---|---|
| FR-4.1 | **目录驱动加载**：启动扫描 `resources/guides/*.md`，从 H1 标题自动提取城市名；新增城市=放置一个文件，零配置 | P0 | 当前覆盖 10 城；日志显示"支持城市 10 个" |
| FR-4.2 | **混合检索**：向量（embedding+余弦）+ BM25 关键词 + RRF 融合；向量不可用自动降级关键词 | P0 | 检索结果带来源/标题/相关度；断网场景降级不报错 |
| FR-4.3 | **持久化向量缓存**：guide_embedding 表按 (source, chunk_title) 唯一、SHA-256 校验内容变更、模型名防维度误用 | P1 | 重启后命中持久化向量，无重复 embedding 调用 |
| FR-4.4 | **攻略卡片解析**：解析 `### 景点名` + `位置/门票/时长/简介/注意` 为结构化卡片（findSpotCard），自动排除餐饮/住宿/贴士块 | P0 | 10 城各解析出 12-13 张卡片；匹配按规范化名称相等/包含取最长 |
| FR-4.5 | RAG 结果缓存：Redis 6h TTL，带标签维度过滤 | P1 | 同城市同查询命中缓存日志 |

### 3.5 数据可信度模块（M5，差异化核心）

| 编号 | 需求 | 优先级 | 验收要点 |
|---|---|---|---|
| FR-5.1 | **攻略卡片事实回写**：生成后按景点名匹配卡片，命中则用卡片的位置/简介/门票强制覆盖 LLM 输出，来源标注"本地攻略" | P0 | 上海自然博物馆地址恒为"北京西路510号"；描述为攻略原文 |
| FR-5.2 | **高德地址绑定**：POI 结果中的真实地址覆盖景点/酒店地址（精确优先，否则取最长包含匹配） | P0 | 无卡片但有 POI 的景点地址为高德真实地址 |
| FR-5.3 | **跨天去重**：景点/餐厅跨天不得重复，重复项用候选池真实 POI 替换 | P0 | 全行程无重复景点/餐厅 |
| FR-5.4 | **餐厅真实性**：餐食必须来自 POI「餐厅」分类或攻略原文；连锁快餐（肯德基/麦当劳等黑名单）自动替换；无候选则标注"LLM建议（需核实）" | P0 | 行程无连锁快餐；餐厅来源可追溯 |
| FR-5.5 | **恶劣天气 Plan B**：预报含雷/暴/雨/雪等字眼的当天，户外景点替换为室内候选（博物馆/商场等），无候选则备注警示 | P0 | 雷暴天行程以室内为主 |
| FR-5.6 | **预算合理性修正**：估算与用户预算偏差 >150% 或 <30% 时，回传 LLM 仅调整价格字段（结构名称不变）重新匹配预算 | P0 | 预算 3000 行程总价在合理区间；极端预算（300/10万）不崩且诚实警示 |
| FR-5.7 | **酒店建模**：按档次给单间价区间（经济 150-250/舒适 300-500/豪华 600-1200），房间数≈ceil(人数/2)；3 人以上人均<150 元警示"可能未按人数配房" | P1 | 3 人舒适型行程酒店价符合 2 间房水平 |
| FR-5.8 | **酒店不得作景点**：与当日酒店同名/包含的景点自动移除 | P1 | 景点列表无酒店 |
| FR-5.9 | **描述交叉检测**：描述中出现其他地点名时剥离相关句子（降级）并提示；地理通名（湾/路/街等）不算误报 | P1 | 无"博物馆写商场介绍"类串味；"三亚湾"不误报 |
| FR-5.10 | **来源标注体系**：景点/餐厅/交通均有 source 标签（高德POI/本地攻略/估算LLM/LLM建议需核实），前端展示可信度 | P1 | 结果页可见每个项目来源 |
| FR-5.11 | **source_notes 防编造**：LLM 不得在数据来源说明中核算预算，含"总额/核算/总预算"的编造行自动清除；预算以系统「预算明细」为单一事实源 | P0 | 说明文字与实际预算明细一致 |

### 3.6 个性化模块（M6）

| 编号 | 需求 | 优先级 | 验收要点 |
|---|---|---|---|
| FR-6.1 | **长期记忆画像**：生成前基于该用户已保存行程（最多 5 条）确定性统计画像：去过城市/最近摘要/节奏偏好/住宿档次/餐饮口味/平均预算（零 LLM 调用） | P0 | 有历史的用户日志出现"用户长期记忆注入" |
| FR-6.2 | **记忆注入**：画像以【用户历史记忆】段注入生成提示词，附规则：重复目的地换新玩法、延续偏好、不得编造 | P0 | 第二次生成同城市时行程与首次有明显区分 |
| FR-6.3 | **记忆可见**：结果页"数据来源说明"出现"已结合你的历史行程定制" | P1 | 前端可见个性化标识 |
| FR-6.4 | **缓存按用户隔离**：结果缓存与计划缓存 key 均含 userId，防止跨用户串味 | P0 | A 用户个性化结果不会被 B 用户命中 |
| FR-6.5 | 记忆安全：只读快照、按用户隔离、构建失败静默降级（无历史用户行为与旧版完全一致） | P0 | 新注册用户生成无异常、无记忆段 |

### 3.7 行程管理模块（M7）

| 编号 | 需求 | 优先级 | 验收要点 |
|---|---|---|---|
| FR-7.1 | 保存行程：POST /trip/save，整份行程（含轨迹）落库，userId 以服务端为准 | P0 | 保存后列表可见 |
| FR-7.2 | 行程列表：GET /trip，当前用户按时间倒序 | P0 | 仅本人行程 |
| FR-7.3 | 行程详情：GET /trip/{trip_id}，附带 Agent 轨迹（供回放） | P0 | 详情含轨迹 |
| FR-7.4 | 删除行程：DELETE /trip/{trip_id}，逻辑删除，仅本人 | P0 | 删除后列表消失 |

### 3.8 导出模块（M8）

| 编号 | 需求 | 优先级 | 验收要点 |
|---|---|---|---|
| FR-8.1 | Markdown 导出：GET /export/{trip_id}/markdown | P1 | 输出可读行程文档 |
| FR-8.2 | PDF 导出：GET /export/{trip_id}/pdf | P1 | 输出 PDF 文件，中文正常 |

### 3.9 前端交互模块（M9）

| 编号 | 需求 | 优先级 | 验收要点 |
|---|---|---|---|
| FR-9.1 | 规划表单：目的地、**日历选择开始/结束日期**（开始禁过去、结束≥开始、最长 7 天联动）、人数、预算、档次、节奏、偏好标签、特殊需求 | P0 | 日历约束生效；改天数联动结束日期 |
| FR-9.2 | Agent 过程动画：实时展示思考轨迹（计划/工具/反思/生成），支持历史轨迹回放（播放/暂停/上一步/下一步） | P0 | 生成中逐步展示；历史详情可回放 |
| FR-9.3 | 结果页：行程摘要、预算明细、按天花费、地图点位（高德）、天气表、每日安排、来源说明 | P0 | 数据完整展示，地图点位可点击 |
| FR-9.4 | 历史列表页：查看/回放/删除/导出 | P1 | 完整闭环 |
| FR-9.5 | 登录/注册页：JWT 持久化（localStorage），拦截未登录跳转 | P0 | 未登录访问跳登录 |

---

## 4. 非功能需求

| 编号 | 类别 | 需求 | 验收要点 |
|---|---|---|---|
| NFR-1 | 性能 | 同参数行程生成**结果缓存命中时秒回**（Redis 30min TTL） | 同参数第二次请求 <1s 返回 |
| NFR-2 | 性能 | think 计划缓存（内存 1h）+ 外部数据缓存（POI 24h/天气 30min/RAG 6h） | 改预算重生成时 Step1/2 秒过 |
| NFR-3 | 性能 | 工具并行执行，单轮工具总耗时 ≤8s；主生成 LLM 超时 90s 兜底 | 工具阶段实测毫秒级（缓存命中） |
| NFR-4 | 安全 | 密码 BCrypt 哈希存储；JWT HS256 密钥经环境变量注入（默认值仅演示用） | 库中无明文密码；密钥不出现在提交文件中 |
| NFR-5 | 安全 | 输入校验（Bean Validation）+ 防伪造（userId 服务端注入）+ 城市校验前置拦截 | 非法输入/伪造归属均被拒绝 |
| NFR-6 | 可靠性 | **优雅降级**：Redis 不可用跳过缓存；Chroma 不可达降级内存余弦；embedding 失败降级 BM25；搜索失败降级；任一外部故障不阻断生成 | 关闭 Redis/断网场景系统仍可生成 |
| NFR-7 | 可靠性 | LLM 调用有超时/失败/断连三套兜底 | 慢响应/断连不挂死 |
| NFR-8 | 可维护性 | 六层分层（controller/agent/service/client/model/repository）；攻略目录驱动扩展；TROUBLESHOOTING.md 沉淀问题 | 新增城市零代码改动 |
| NFR-9 | 可扩展性 | 新工具接入只需在智能体工具目录注册；新数据源可插拔 | 工具目录可配置 |
| NFR-10 | 兼容性 | Windows/Docker 双环境可运行；现代浏览器兼容 | 部署文档可复现 |
| NFR-11 | 隐私 | 行程/画像严格按 userId 隔离；只读快照注入，不持久化用户画像 | 数据不跨用户泄露 |
| NFR-12 | 可用性 | SSE 进度实时反馈、错误信息明确（400 校验/生成失败原因） | 用户能理解"为什么失败" |

---

## 5. 数据需求

### 5.1 数据模型（MySQL，4 张表）

| 表 | 关键字段 | 用途 |
|---|---|---|
| users | id, username(唯一), password_hash(BCrypt), nickname, deleted | 用户账号 |
| trip_record | trip_id(唯一), destination, itinerary_json(JSON 整份行程), user_id, deleted | 行程记录（长期记忆数据源） |
| agent_trace | trip_id, step, thought, action, observation, tool_calls(JSON) | Agent 推理轨迹（回放/答辩展示） |
| guide_embedding | source, chunk_title, content_hash(SHA-256), model, dim, vector(LONGBLOB) | RAG 向量持久化缓存 |

### 5.2 关键数据流（行程生成）

```
用户输入(TripRequest) → 城市校验(四道防线) → 注入用户上下文(userId+记忆画像)
→ [缓存命中?] → SSE 秒回缓存行程
→ [未命中] ReAct 智能体: THINK(LLM计划|计划缓存) → ACT(并行工具: 高德POI/天气/RAG/搜索)
→ OBSERVE(规则判足够,跳过LLM) → GENERATE(LLM生成JSON)
→ 校验层(去重/PlanB/预算/地址绑定/攻略卡片回写/点名校验/来源标注)
→ 预算重算 → SSE 推送 → (用户保存) → 落库 trip_record + agent_trace
```

### 5.3 缓存设计（三层）

| 层 | 存储 | 粒度 | TTL |
|---|---|---|---|
| 外部数据 | Redis | 城市+类别 | POI 24h / 天气 30min / RAG 6h |
| think 计划 | 内存 | 城市+偏好+用户 | 1h |
| 生成结果 | Redis | 参数哈希+用户 | 30min |

---

## 6. 接口需求

### 6.1 REST 接口清单

| 方法 | 路径 | 鉴权 | 说明 |
|---|---|---|---|
| POST | /auth/register | 否 | 注册 |
| POST | /auth/login | 否 | 登录，返回 JWT |
| POST | /trip/generate | 是 | 生成行程（无轨迹，兼容旧调用） |
| POST | /trip/generate-with-trace | 是 | 生成行程（带轨迹） |
| POST | /trip/generate-stream | 是 | **SSE 流式生成**（主入口） |
| GET | /trip | 是 | 历史行程列表 |
| GET | /trip/{trip_id} | 是 | 行程详情（含轨迹） |
| POST | /trip/save | 是 | 保存行程 |
| DELETE | /trip/{trip_id} | 是 | 删除行程 |
| GET | /export/{trip_id}/markdown | 是 | Markdown 导出 |
| GET | /export/{trip_id}/pdf | 是 | PDF 导出 |
| GET | /weather/forecast | 是 | 天气查询 |

### 6.2 SSE 事件协议（/trip/generate-stream）

| 事件 | 数据 | 说明 |
|---|---|---|
| progress | {phase, message} | 阶段进度（think/act/assess/generate） |
| step | AgentTraceStep | 轨迹步骤（计划/执行/反思/生成） |
| itinerary | Itinerary | 最终行程 |
| done | {token_usage, cached?} | 完成（含 token 统计） |
| error | {message} | 失败原因 |

---

## 7. 约束与假设

1. 定位为**单城市行程规划**；跨城市多目的地串联不在当前范围（后续独立评估）；
2. 攻略知识库当前覆盖 10 城，未覆盖城市走"高德地址兜底 + 描述标注需核实"降级路径；
3. 外部 API（大模型/高德/天气/搜索）需有效 Key，Key 通过环境变量注入；
4. 行程内容基于真实数据生成，但门票/价格仅供参考，不构成消费承诺；
5. 港澳地区受高德地址数据质量限制，地址准确度低于大陆城市（已知局限，通过补攻略缓解）。

---

## 8. 验收标准（对标答辩）

### 8.1 功能验收（可演示脚本）

| # | 演示场景 | 对应需求 | 通过标准 |
|---|---|---|---|
| A1 | 注册→登录→生成上海 3 天行程 | FR-1/3.3.4 | 全程 4 阶段动画，结果完整 |
| A2 | 额外要求"想去自然博物馆和东方明珠" | FR-3.6/5.1 | 两景点在行程，地址=北京西路510号/世纪大道1号 |
| A3 | 额外要求"想去上海野生动物园"（攻略无） | FR-3.6 | 景点进入行程，地址真实（高德定向查询） |
| A4 | 额外要求"想去杭州灵隐寺"（跨城） | FR-3.6 | 明示"未能安排+原因"，不硬塞 |
| A5 | 假城市"火星市" | FR-2.2 | 400 拒绝，零 token 消耗 |
| A6 | 无 RAG 城市（苏州/南京） | FR-4/约束2 | 照常生成，地址高德真实，标注来源 |
| A7 | 同参数二次生成 | NFR-1 | 秒回并提示缓存命中 |
| A8 | 保存后同账号换城市再生成 | FR-6 | 日志"记忆注入"，结果页出现个性化标识 |
| A9 | 预算 300 元极端值 | FR-5.6 | 不崩，诚实警示 |
| A10 | 历史行程回放/导出 | FR-7/8 | 轨迹动画回放、PDF 中文正常 |

### 8.2 答辩追问点（建议准备）

| 追问 | 答案要点 |
|---|---|
| 与直接问 ChatGPT 有何区别？ | 可控过程（工具定义/数据来源可校验）+ 事实锚定（生成后可强制修正）+ 个性化记忆 |
| RAG 解决了什么？ | 幻觉（有据可依）、私有性（本地攻略）、可维护（目录驱动）；不解决时效性 |
| "RAG 命中≠覆盖"怎么理解？ | 命中数是检索结果，覆盖率才是可锚定的景点占比；解法=补覆盖+结构化卡片+强制回写 |
| 为什么不用现成 Agent 框架？ | 自研 ReAct 可完全控制循环/超时/降级/token 成本，且是本项目核心工作量 |
| 记忆会不会污染生成？ | 只读快照+按用户隔离+失败降级+用户主动保存才入库 |

---

## 9. 需求追踪（简版）

| 需求 | 模块 | 实现载体（主要类/文件） |
|---|---|---|
| FR-1.x | 用户管理 | AuthController / JwtAuthInterceptor / UserContext / AuthService |
| FR-2.x | 城市校验 | CityValidator / CityValidationException |
| FR-3.x | 行程生成 | TravelAgent / TripStreamController / ItineraryGenerator / AgentCallback |
| FR-4.x | RAG | RagService / GuideEmbeddingRepository / ChromaVectorStore / guides/*.md |
| FR-5.x | 数据可信度 | ItineraryValidator / MapEnrichmentService / AmapClient |
| FR-6.x | 个性化 | UserProfileService / TripRecordService |
| FR-7.x | 行程管理 | TripController / TripRecordService |
| FR-8.x | 导出 | ExportController / PdfExportService |
| FR-9.x | 前端 | Home.vue / AgentProcess.vue / Result.vue / History.vue / api.ts |

---

*本说明书随系统迭代维护；重大需求变更须同步更新本文档并记录于 TROUBLESHOOTING.md。*
