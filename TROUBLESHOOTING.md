# 技术难点与解决记录（AI旅游助手 · 答辩素材库）

> **用途**：记录开发中遇到的每一个真实问题——现象 / 根因 / 解决 / 答辩话术。
> 答辩时，"你解决过什么真问题"比"你做了多少功能"值钱得多，这份文档就是深度的证据。
>
> **维护约定**：每解决一个问题，按下列格式追加一条。AI 在后续开发中会自动维护此文档。

---

## 一、行程数据可信度（LLM 幻觉治理）——本系统核心差异化

### 1.1 景点地址张冠李戴（最严重问题）
- **现象**：上海行程里，上海自然博物馆地址写成"人民大道185号"（那是人民广场）、四行仓库写成"中山东二路1号"（外滩）、上海市历史博物馆写成"南京东路558号"（南京路步行街）。描述也全串：给博物馆写"商业第一街""万国建筑博览群"。
- **根因**：LLM 把相邻地标的地址/简介套到了博物馆身上；POI 工具按类别撒网（每类前15条），冷门景点（博物馆）根本不在 POI 候选里，模型无据可依只能凭记忆编。
- **解决**（三层防线）：
  1. **提示词约束**：地址必须忠于景点本身，禁止写相邻街区；
  2. **高德 POI 地址绑定**：用高德返回的真实地址覆盖 spot.address；
  3. **攻略卡片事实回写（最关键）**：攻略结构化成卡片库（位置/门票/时长/简介/注意），生成后按景点名匹配，命中就用人工整理的数据强制覆盖地址和简介——**有攻略覆盖的景点，事实 100% 来自人工数据，不可能错**。
- **答辩话术**："我们发现了 LLM 生成内容的可信度问题——模型会为错误的地址编写合理化描述。所以做了事实锚定：攻略结构化成卡片，生成后强制回写。这是从'模型说什么就是什么'到'数据说了算'的关键改造。"

### 1.2 预算编造 / 前后矛盾
- **现象**：行程的"数据来源说明"声称"总额3984元、含应急备用金2144元"，但系统"预算明细"实际算出来是1450元——同一份行程两套金额，LLM 在说明里编了一套不存在的核算。
- **根因**：source_notes 是 LLM 自由生成的字段，它把预算明细也当作创作内容编了一遍，没有和系统真实计算对齐。
- **解决**：`cleanFakeBudgetNotes` 删除含"总额/核算/总预算"的编造行 + 提示词规则"source_notes 禁止核算预算，一切以系统「预算明细」为准"。
- **答辩话术**："预算必须单一事实源——LLM 只能说明数据来源，不能参与金额核算，金额一律由后端代码汇总。"

### 1.3 酒店被当成景点列出
- **现象**：行程第 3 天的"主要景点"竟然是"登巴客栈"——酒店出现在景点列表里。
- **根因**：LLM 混淆了 spots 和 hotel 两个字段的职责。
- **解决**：校验层把与当日酒店同名/包含的景点直接移除 + 提示词规则"酒店只能写在 hotel 字段"。
- **答辩话术**：数据模型校验——输出结构由程序兜底，不信任模型自律。

### 1.4 餐厅凭空编造
- **现象**："巴奴毛肚火锅""大壶春"等餐厅不在任何 POI/攻略数据里，但出现在行程中，且说明文字宣称"餐厅均严格选自 POI"——自相矛盾。
- **根因**：用户选"美食"偏好时 POI 没查餐厅类别，LLM 只能自己"推荐"。
- **解决**：餐食不在 POI 餐厅池时，用真实 POI 餐厅自动替换；提示词强制"餐厅必须来自【POI数据】餐厅分类"。
- **答辩话术**："让模型的每个输出字段都有可追溯的数据来源，查不到来源就替换或标注'需核实'。"

### 1.5 多人群住宿按单间计价
- **现象**：3 人行程只按 1 间房算住宿（120 元/人/晚），与"舒适型"档次明显不符。
- **解决**：提示词按档次建模（经济 150-250/间/晚、舒适 300-500、豪华 600-1200），房间数≈ceil(人数/2)；校验层检测 3 人以上人均低于 150 元时警示"可能未按人数配房"。
- **答辩话术**：预算合理性建模——价格模型由规则约束，不是模型自由发挥。

---

## 二、RAG 知识库（从"接了 RAG"到"RAG 真的好用"）

### 2.1 RAG 命中 ≠ 覆盖（关键认知）
- **现象**：上海明明命中 5 条 RAG，但行程里的自然博物馆/四行仓库/历史博物馆全部张冠李戴。
- **根因**：RAG"命中 N 条"只代表检索到 N 段文字，**不代表覆盖了行程里选的景点**。当时攻略只有 5-6 个完整景点卡片，而高德 POI 返回约 20 个景点——模型选到没卡片的景点就无从依据。
- **解决**：① 10 城攻略全部补齐到 12-13 个完整卡片（覆盖高德常返回的热门景点）；② 攻略结构化解析成卡片库；③ 生成后按卡片强制回写事实。
- **答辩话术**："我们发现 RAG 命中率和覆盖率是两回事。命中数高 ≠ 结果可靠，真正的解法是让攻略覆盖常见选择，并把覆盖的部分变成强约束，而不是软参考。"——这是市面上很多 RAG demo 都没意识到的问题。

### 2.2 新增城市 RAG 不生效
- **现象**：加了 `shanghai_guide.md`，但上海仍显示"RAG 无"。
- **根因**：`RagService` 有硬编码门禁——`isKnownCity()` 查写死的 6 城配置、文件名映射写死拼音，新文件白加。
- **解决**：目录驱动——启动时扫描 `guides/*.md`，从 H1 标题自动提取城市名，加城市 = 放一个文件，零配置。
- **答辩话术**："知识库是目录驱动的单一真相源，扩展城市不需要改代码。"（附：新增 4 城，6→10）

### 2.3 混合检索的降级设计
- **设计**：向量(embedding+余弦) + BM25 关键词 + RRF 融合；向量不可用自动降级关键词；Chroma 不可达自动降级内存余弦；持久化向量缓存（guide_embedding 表，SHA-256 校验内容变更）。
- **答辩话术**："检索是'可用性优先'——任何一层挂了都优雅降级，绝不让一次外部故障毁掉整个生成。"

---

## 三、性能（从"卡很久"到"秒回"）

### 3.1 生成卡在"AI 正在思考"
- **现象**：前端无限等待。
- **根因**：主生成 LLM 调用无超时；45 秒硬超时对大城市长行程又不够。
- **解决**：加超时保护（主生成 90s、JSON 修正 20s），超时抛异常走 SSE error 反馈，前端不再死等。
- **答辩话术**："LLM 是慢服务，必须有超时、失败、断连三套兜底，这是生产级系统的基本素养。"

### 3.2 Step 3（反思）白调一次 LLM
- **现象**：4 步流程中第 3 步每次都等很久。
- **根因**：数据是否足够**早已由规则判定**，LLM 反思只是写一段展示文字，纯浪费一次串行调用。
- **解决**：规则判定足够时跳过 LLM，直接用规则结论文本。
- **答辩话术**："能确定性算的绝不调模型——ReAct 的每一步都要问'这一步真的需要 LLM 吗'。"

### 3.3 连续规划同城市还是很慢
- **现象**：外部数据明明有缓存（工具执行仅 4ms），但整体流程还是几十秒。
- **根因**：慢的是 LLM 串行调用（think/reflect/generate），不是数据获取。
- **解决**：三层缓存——
  1. 外部数据（Redis 城市级：POI 24h/天气 30min/RAG 6h，原有）
  2. think 计划（内存 1h，城市+偏好级，新增）
  3. 生成结果（Redis 30min，参数级 + userId 隔离，新增）
- **答辩话术**："缓存分三层、按粒度隔离：数据按城市、计划按城市+偏好、结果按参数+用户。同一份请求第二次秒回，改参数只重算必要的部分。"

---

## 四、用户需求保障（"用户说了算"）

### 4.1 点名景点被静默忽略
- **现象**：用户在"额外要求"里说"想去上海野生动物园"（真实存在但攻略没有），行程里没有它，且系统一声不吭。
- **根因**：specialNotes 只是一段文本提示，POI 工具按类别撒网查不到冷门景点，模型无据可依就忽略，且无任何反馈机制。
- **解决**：
  1. **定向查询**：从特殊需求提取景点名（攻略卡片反向匹配 + 清洗后高德定向查询），单独成"指定景点"数据类别；
  2. **硬保证**：提示词规则"点名景点必须进 spots"+ 数据摘要【用户点名景点】段；
  3. **做不到明说**：生成后校验，点名景点未进行程 → 结果里明确写"⚠️ 你指定的景点未能全部安排：XX（原因可能是数据源未收录或不在本城市）"。
- **答辩话术**："用户指定是最高优先级，数据源只是为了让满足需求的信息更可靠——做不到时系统必须告诉用户为什么，而不是假装没听见。"

### 4.2 跨城指定景点的边界
- **决策**：本次目的地之外的城市景点不硬塞进行程，提示"不在本城市"；多城市串联作为后续独立需求评估。
- **答辩话术**："系统边界要诚实——单城市生成就明确单城市，不为了显得强大而做半吊子的跨城。"

---

## 五、个性化 / 长期记忆（方向二）

### 5.1 从"无状态智能体"到"有记忆智能体"
- **背景**：每次生成都是失忆的——行程存进 trip_record 但从不回读（只写不读）。
- **实现**：
  1. `UserProfileService`：基于历史行程确定性统计画像（去过城市/最近摘要/节奏/住宿档次/餐饮口味/平均预算），零 LLM 调用；
  2. 生成前注入【用户历史记忆】提示词段（重复目的地换新玩法 / 延续偏好 / 不得编造）；
  3. 结果页标注"✨ 已结合你的历史行程定制"；
  4. **缓存按 userId 隔离**（结果缓存 + 计划缓存 key 都含 userId），防止 A 的个性化结果串给 B。
- **关键工程点**：userId 从 ThreadLocal 取——必须在主线程捕获，异步线程读不到。
- **答辩话术**："短期工作记忆（本轮 CollectedData）和长期记忆（trip_record 画像）分层；记忆是只读快照 + 按用户隔离 + 失败降级，不会反向伤害生成质量。"

---

## 六、工程与环境坑（踩过就记）

- **Maven 沙箱编译**：bash 版 `mvn` 坏（boot/ 缺 classworlds jar），但 `mvn.cmd` 可用——`D:\apache-maven-3.9.16-bin\...\bin\mvn.cmd -q compile`。
- **改 Java 必须重启后端**：无 devtools 热替换，`mvn test` 也不热替换，别误判"改了没生效"。
- **Windows SERVER_PORT=0** 环境变量会致随机端口，用 `--server.port=8080` 锁回。
- **GitHub 推送被网络拦截**（国内 443 连不上）：需要代理，或临时用镜像 `git config --global url."https://ghfast.top/https://github.com/".insteadOf "https://github.com/"`。
- **ItineraryGeneratorTest 构造签名**：实现类加依赖后测试类必须同步，否则 IDE 报红。

---

## 待办 / 已规划（未实施）
- 方向三：ECharts 可视化（预算饼图/行程时间轴/天气曲线）——答辩视觉冲击最大，建议答辩前做。
- 方向四：工程加固（JWT 强密钥 / Docker 全栈一键编排 / 搜索重试降级）。
- 跨城多目的地串联生成（独立需求评估）。
