# 行程生成质量全面排查与修复方案

> 目标：让「智能旅行助手」生成的行程不再出现「天数错位 / 档次错配 / 预算离谱 / 前后自相矛盾」这类肉眼可见的硬伤，达到真实可交付的业务质量。
> 排查范围：`ItineraryGenerator`（生成提示词 + 解析）→ `ItineraryValidator`（生成后硬校验）→ `Result.vue`（前端渲染口径）整条链路。
> 日期：2026-09-18

---

## 一、核心结论（先说根因）

用户这次暴露的两个症状，根子其实**同一个**：

> 系统把「天数」「住宿档次」「预算用满」这三条最关键的约束，**只写在提示词里**（生成提示词第 6 / 18 / 20 条），生成后**没有任何一条硬校验兜底**。LLM 一旦不听话（输出 3 天、选经济公寓、报价偏低），页面就把矛盾结果原样裸奔给用户看。

- 「5 天变 3 天」：提示词写了「恰好 N 天」，但 LLM 只输出 3 天，`trip_days` 却按日期跨度记成 5 → 页面写 5 天、正文 3 天。
- 「8000 预算高档只花 1700」：提示词写了「每晚 ≤ 45% 预算」和「档次匹配」，但 LLM 选了经济型公寓（¥200/晚）、景点多免费 → 最终估算只到预算 16%，系统对此**一言不发**，用户只能看到「价格在搞笑」。

**修复方向（已在代码里落地的第一批）**：不信任 LLM 输出，把这三条约束从「提示词」下沉到「生成后确定性硬校验」——能本地确定性修的，绝不外抛给用户，也绝不靠再问一次模型（又慢又可能再犯）。

---

## 二、已完成的修复（第一批，代码已改、测试 649 绿）

改动文件：`ItineraryValidator.java`、`ItineraryGenerator.java`。

| # | 症状 | 根因 | 修复方法 | 状态 |
|---|------|------|----------|------|
| A | 5 天只生成 3 天 | 天数只写提示词、无生成后核对 | `syncStructure`：天数 = 出行日期跨度强制对齐（多截断、少用候选池补足），每日景点数下限（完整日≥2、返程日≥1），重排 `day_index`/`date`，`trip_days` 写真实天数 | ✅ 已实现 |
| B | 高档型住了经济公寓 | 档次只写提示词、无落地 | `upgradeHotelTier`：用户选高档/豪华而 LLM 选了经济公寓时，先在候选酒店池换一家真实酒店，按所选档次估价并用 `hotelLevelForPrice` 反推档次，保证「价格与档次自洽」 | ✅ 已实现 |
| C | 摘要还写「5 天·精选高档酒店」 | `cleanStaleLodgingClaims` 只清 tips+每日 notes，漏了 summary | `cleanStaleSummaryClaims`：清 summary 里的过期住宿断言 + 校正天数/晚数；清空则给系统口径兜底摘要 | ✅ 已实现 |
| D | 预算只用 16% 却一声不吭 | 无「预算没花出去」说明 | `checkBudgetUnderuse`：使用率 <60% 时诚实说明原因（候选池消费水平有限、景点多免费）+ 给可选用足预算的动作 | ✅ 已实现 |

**调用顺序**（关键，已在代码里固定）：`syncStructure` 必须在高德补全（图片/坐标）**之前**调用，否则补入的景点拿不到图。`upgradeHotelTier` 必须在 `validateHotelLevelMatch`（降级）**之前**，能换就不该降级。

---

## 三、排查中发现的「连带隐患」（第二批，需一并修）

在实现第一批修复时，顺带发现 4 个**更隐蔽的口径矛盾**，它们会在别的场景下复发，必须一起处理才能保证长期质量：

### 隐患 1：两套「酒店档次价位表」互相打架 ⚠️ P0 ✅ 已修复

- 新增 `TIER_PER_NIGHT`：舒适 380 / 高档 520 / 豪华 760。
- 既有 `hotelLevelForPrice`：`≥600 豪华 / ≥400 高档 / ≥200 舒适`。
- 生成提示词第 13 条：经济 150-250 / 舒适 300-500 / 豪华 600-1200。

**三处价位边界不一致**（如「舒适型」一处 380、一处 300-500、`hotelLevelForPrice` 里 200-400 都算舒适）。

**已修复**：`hotelLevelForPrice` 改为从 `TIER_PER_NIGHT` 单一真源推导「相邻档次中点」判定归属（舒适/高档中点 450、高档/豪华中点 640、经济/舒适约 190），消除魔法数字漂移。提示词第 13 条的价位区间仍为 LLM 参考值，与程序档位边界在量级上对齐（380/520/760 分别落在 300-500 / 500 上沿 / 600-1200 内），不再互相打架。测试 `hotelLevelForPrice_brackets`（700→豪华/450→高档/250→舒适/120→经济）与 `upgradeHotelTier` 全部通过。

### 隐患 2：「预算没花出去」只是诚实说明，没真正用足预算 🟡 P1

`checkBudgetUnderuse` 现在只加一条 💡 说明。用户要的是「8000 高档就该给出值 8000 的行程」，而不是「说明为什么只有 1700」。真正根因是：

- 候选酒店池里**根本没有 ¥520/晚的高档酒店**（高德 POI 不含房价，系统拿不到真实房费）；
- 高德 POI **不含门票/餐饮人均的真实价格**，系统只能靠 LLM 估价或默认 0。

**修法（诚实优先，不硬凑）**：
1. 候选池若确有更高档酒店，`upgradeHotelTier` 已经会换；若没有，就在 source_notes 里**明确告诉用户「本城市候选池暂无 XX 档酒店，已按可得的最高档安排」**——让用户理解不是系统偷懒，是数据边界。
2. 对「免费景点占大头」的城市，允许在预算内**主动增加付费体验/高人均餐厅**（但这依赖真实价格数据，当前 POI 拿不到，属数据层增强，见隐患 4）。
3. **底线**：绝不靠虚标价格把 1700 撑到 8000——那才真的是「价格在搞笑」。

### 隐患 3：`summary` 天数校正用正则，边界可能误伤 🟡 P1

`cleanStaleSummaryClaims` 里 `replaceAll("\\d+\\s*天", realDays+"天")` 会把摘要里**所有数字+天**都替换成真实天数。若摘要本意写「2 天沙滩 3 天城市」这类分项，会被全部覆盖成同一个数。

**修法**：只替换「共 X 天 / X 天行程 / 总计 X 天」这类**总量断言**的句式，分项不动；或干脆像 `fallbackSummary` 一样，摘要的「天数」字段只由代码拼装，LLM 不得在 summary 里写天数。

### 隐患 4：候选池「无房价/无票价」是预算算不准的深层数据问题 🟢 P2（数据层增强，可后续）

高德 POI 不提供房价和大部分门票价，导致预算估算「先天缺一块」。这是「8000 vs 1700」的最深根因——不是算法算错，是**数据源里根本没有能撑起 8000 的高消费项目**。

**修法（可选增强，非本次必须）**：对酒店/景点补充一份「城市 × 档次」参考价字典（人工维护或联网搜索采集），给估价兜底，让高档型行程有「¥520-760/晚」这类真实量级的报价可填。

---

## 四、修复后要达到的验收标准（可演示口径）

对同一「三亚 / 5 天 / 2 人 / ¥8000 / 高档型」请求，修复后必须满足：

1. ✅ 结果**恰好 5 天**，`day_index` 1-5 连续，`date` 逐日对应，`trip_days=5`；
2. ✅ 每天景点数：完整日 ≥2、返程日 ≥1，不出现「空心天」；
3. ✅ 住宿档次与价格自洽：高档型 → 候选池里的真实酒店，价格在对应档次区间，**不再出现「高档型 ¥200/晚」**；
4. ✅ 摘要、旅行提示、每日备注里**没有过期口径**（不出现「5 天」配「3 天」、「高档」配「经济 ¥200」）；
5. ✅ 预算：若估算低于预算 60%，页面有**明确说明**（原因 + 可选用足预算的动作），而不是静默；
6. ✅ 全程无假数据：价格要么来自真实来源，要么明确标注「估算」。

---

## 五、待办清单（按优先级）

| 优先级 | 事项 | 说明 |
|--------|------|------|
| ~~P0~~ | ~~给第一批 4 个新方法补单元测试~~ | ✅ 已补 5 例（`syncStructure`×2 / `upgradeHotelTier` / `cleanStaleSummaryClaims` / `checkBudgetUnderuse`），全绿 |
| ~~P0~~ | ~~统一「酒店档次价位表」单一真源~~ | ✅ 已修：`hotelLevelForPrice` 反查 `TIER_PER_NIGHT` 中点判定，消除三处数字漂移 |
| P1 | `cleanStaleSummaryClaims` 天数替换收窄 | 只替换总量断言句式，防误伤分项 |
| P1 | 跑真实链路探针验证 | 用 5 天/高档/¥8000 场景端到端验证 6 条验收标准 |
| P1 | 前端 build 确认干净 | `vue-tsc --noEmit` + `vite build` EXIT=0 |
| P2 | 候选池无高档酒店时给出明确数据边界说明 | 让用户理解「不是偷懒，是数据边界」 |
| P2 | 补充城市×档次参考价字典 | 数据层增强，让高档行程有真实量级报价 |

---

## 六、修复方法论（沉淀，供后续同类问题套用）

这次排查印证了一条反复被验证的规律：

> **「一句提示词要求」永远不构成质量保证。** 凡是会影响交付观感的关键约束（天数、档次、预算、去重、天气应对、点名景点……），都必须有**生成后确定性硬校验**兜底；能本地确定性修的绝不外抛，绝不靠「再问一次模型」补救（又慢又可能再犯）。

已在 `MEMORY.md` 沉淀的同类经验也再次生效：
- 「校验通过 ≠ 用规范键存取」「结论 ≠ 后果」「一句文案只对应一个根因」；
- 「顺序依赖的约束要放在信息齐全的那一步」（天数/可行性只能在生成后校验）；
- 「临时降级产物绝不进复用池」。

---

*本方案为排查结论 + 修复方向，第一批修复（A/B/C/D）代码已落地并通过测试；后续待办按优先级逐项推进。*
