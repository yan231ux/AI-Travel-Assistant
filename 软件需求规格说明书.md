# 基于 ReAct 智能体的个性化旅行规划系统 软件需求规格说明书

| 项目名称 | 基于 ReAct 智能体的个性化旅行规划系统 |
|---|---|
| 文档编号 | SRS-001 |
| 版本号 | V1.0 |
| 编写日期 | 2026-08-30 |
| 编制依据 | GB/T 9385-2008《计算机软件需求规格说明规范》、GB/T 8567-2006《计算机软件文档编制规范》 |

---

## 1. 引言

### 1.1 编写目的

本文档规定"基于 ReAct 智能体的个性化旅行规划系统"（以下简称"本系统"）的软件需求，包括功能需求、非功能需求、外部接口需求与数据需求，作为后续设计、开发、测试与验收的依据。

本文档的预期读者包括：项目开发人员、测试人员、项目评审人员及指导教师。

### 1.2 项目背景

旅行规划涉及目的地、日期、天气、景点、交通、住宿、预算等多维信息，人工规划效率低、信息分散。现有通用大语言模型虽可生成行程，但存在生成内容不可信（如景点地址与描述错位、预算金额前后矛盾）、依赖模型内置知识导致时效性与私有性不足等问题。

本系统以自研 ReAct 智能体为核心，结合本地知识库检索增强生成（RAG）技术，面向个人游客提供行程自动生成服务，并对生成结果实施数据可信度校验，保障输出内容的准确性与可追溯性。

### 1.3 术语与缩写

| 术语/缩写 | 定义 |
|---|---|
| ReAct | Reasoning + Acting，智能体"思考—行动—观察"多轮决策循环模式 |
| RAG | Retrieval-Augmented Generation，检索增强生成 |
| RRF | Reciprocal Rank Fusion，倒数排名融合算法，用于多路检索结果融合排序 |
| BM25 | 经典关键词稀疏检索算法 |
| Grounding | 事实锚定，使用可信数据源对模型输出进行约束与回写 |
| SSE | Server-Sent Events，服务器推送事件，用于流式响应 |
| POI | Point of Interest，兴趣点（高德地图地点数据） |
| 攻略卡片 | 从攻略文档解析出的结构化景点条目，包含位置、门票、时长、简介、注意等字段 |
| JWT | JSON Web Token，无状态身份认证令牌 |
| BCrypt | 密码哈希算法，用于安全存储用户口令 |

### 1.4 参考资料

- GB/T 9385-2008 计算机软件需求规格说明规范
- GB/T 8567-2006 计算机软件文档编制规范
- 高德地图开放平台 Web 服务 API 文档
- 阿里云百炼（DashScope）大模型 API 文档
- Open-Meteo 天气预报 API 文档

---

## 2. 总体描述

### 2.1 产品概述

本系统为 B/S 架构的 Web 应用。用户输入目的地、旅行日期、人数、预算、偏好及额外要求后，系统通过自研 ReAct 智能体自动调度各类数据工具（本地知识库检索、天气查询、地图兴趣点查询、联网搜索），完成数据采集与行程编排，并以结构化行程（每日景点、餐饮、住宿、交通、预算明细）形式返回。

系统核心特征：

1. **智能体化**：行程生成由自研 ReAct 智能体自主决策完成，非单次模型调用；
2. **知识库支撑**：内置多城市人工整理的攻略知识库，通过混合检索为生成提供事实依据；
3. **结果可信**：对模型输出实施多级校验与事实回写，降低内容错位与编造；
4. **个性化**：基于用户历史行程构建用户画像，使生成结果延续用户偏好；
5. **过程透明**：生成过程通过流式推送实时呈现，用户可观察智能体决策轨迹。

### 2.2 产品功能

| 编号 | 功能模块 | 功能概述 |
|---|---|---|
| F1 | 用户管理 | 用户注册、登录、身份鉴权、数据隔离 |
| F2 | 城市校验 | 目的地合法性校验，非法目的地直接拒绝 |
| F3 | 行程生成 | ReAct 智能体多轮决策、多工具并行采集、流式生成行程 |
| F4 | 知识库检索 | 本地攻略的向量/关键词混合检索、攻略卡片解析 |
| F5 | 数据可信度保障 | 输出校验、事实回写、预算修正、来源标注 |
| F6 | 个性化服务 | 基于历史行程的用户画像构建与注入 |
| F7 | 行程管理 | 行程的保存、查询、删除 |
| F8 | 行程导出 | 行程文档导出（Markdown、PDF） |
| F9 | 前端交互 | 规划表单、生成过程展示、行程结果展示、历史回放 |

### 2.3 用户特征

| 用户类别 | 特征 | 使用目的 |
|---|---|---|
| 注册用户 | 个人游客 | 登录后生成、保存、查看、导出行程 |
| 未登录用户 | 匿名访问者 | 仅可访问登录与注册页面 |
| 知识库维护者 | 系统维护人员 | 通过添加攻略文档文件扩展知识库覆盖范围 |

### 2.4 运行环境

| 项目 | 要求 |
|---|---|
| 服务端 | Windows / Linux 操作系统，JDK 21，Spring Boot 3.3.5 |
| 数据库 | MySQL 8.0（通过 Docker Compose 编排，对外端口 3310） |
| 缓存中间件 | Redis 7（可选部署，对外端口 6379，不可用时系统自动降级） |
| 向量数据库 | ChromaDB（可选加速层，不可达时自动降级为内存计算） |
| 前端 | 现代浏览器（Chrome、Edge 等），Vue 3 + Vite + Ant Design Vue |
| 外部依赖 | 大语言模型 API、高德地图 API、Open-Meteo 天气 API、Bing 搜索 API（密钥通过环境变量注入） |

### 2.5 设计约束

1. 系统采用前后端分离架构，前端通过 HTTP/SSE 与后端交互；
2. 后端采用分层架构：控制层、智能体层、业务层、外部客户端层、模型层、数据访问层；
3. 所有外部 API 密钥通过环境变量注入，不写入源码；
4. 用户标识（user_id）由服务端从认证上下文解析，不接受客户端传入。

### 2.6 假设与依赖

1. 本系统面向单城市行程规划；多城市串联行程不在当前需求范围内；
2. 攻略知识库当前覆盖 10 个城市；未覆盖城市通过地图数据与联网搜索降级支持；
3. 行程中的门票与价格信息来源于公开数据，仅供规划参考；
4. 外部服务（大模型、地图、天气、搜索）需具备有效调用凭证；
5. 部分港澳地区景点地址受外部数据源质量限制，准确度可能低于内地城市。

---

## 3. 具体需求

### 3.1 外部接口需求

#### 3.1.1 用户接口

| 编号 | 接口 | 说明 |
|---|---|---|
| UI-1 | 登录/注册页面 | 用户输入用户名与口令完成注册或登录 |
| UI-2 | 规划表单页面 | 输入目的地、日期、人数、预算、档次、偏好等参数 |
| UI-3 | 生成过程页面 | 实时展示智能体思考轨迹，支持历史轨迹回放 |
| UI-4 | 行程结果页面 | 展示行程摘要、每日安排、预算明细、地图点位、来源说明 |
| UI-5 | 历史行程页面 | 展示当前用户已保存行程，支持查看、回放、删除、导出 |

#### 3.1.2 软件接口

| 编号 | 外部服务 | 用途 | 调用方式 |
|---|---|---|---|
| SI-1 | 高德地图 Web 服务 | 兴趣点搜索、地理编码、路线规划 | REST API |
| SI-2 | 大语言模型服务（DashScope） | 智能体决策与行程生成 | REST API |
| SI-3 | Open-Meteo 天气服务 | 目的地逐日天气预报 | REST API |
| SI-4 | Bing 搜索服务 | 目的地攻略信息补充检索 | REST API |

#### 3.1.3 通信接口

| 编号 | 接口 | 协议 | 说明 |
|---|---|---|---|
| CI-1 | REST API | HTTP/HTTPS | 常规业务请求，认证方式为 JWT Bearer Token |
| CI-2 | 流式生成接口 | HTTP + SSE | 行程生成过程的事件流推送 |

### 3.2 功能需求

> 需求编号规则：FR-模块-序号。优先级定义：P0 为核心功能，缺失则系统不可用；P1 为重要功能，影响系统质量；P2 为增强功能。
> 每条需求给出"验证"说明，作为需求确认与测试依据。

#### 3.2.1 用户管理

| 编号 | 需求描述 | 优先级 | 验证 |
|---|---|---|---|
| FR-USER-01 | 系统应支持用户注册。用户名唯一，口令经 BCrypt 哈希后存储，禁止明文保存 | P0 | 注册成功入库；重复用户名返回错误提示；数据库中无明文口令 |
| FR-USER-02 | 系统应支持用户登录。校验通过后签发 JWT（HS256，有效期 7 天），失败返回认证错误 | P0 | 正确凭证返回令牌；错误凭证拒绝登录 |
| FR-USER-03 | 系统应对受保护接口进行鉴权。未携带有效令牌的请求返回 401 | P0 | 未登录访问行程相关接口返回 401 |
| FR-USER-04 | 系统应按用户隔离数据。行程的保存、查询、删除均限定当前登录用户，忽略请求体中的用户标识 | P0 | 用户 A 无法访问用户 B 的行程数据 |

#### 3.2.2 城市校验

| 编号 | 需求描述 | 优先级 | 验证 |
|---|---|---|---|
| FR-CITY-01 | 系统应对目的地进行合法性校验，采用多层防线：白名单与别名匹配、后缀归一化、编辑距离纠错、行政区划级地理编码兜底 | P0 | 合法城市（含别名、带"市"后缀写法）可通过；非法或虚构城市被拒绝 |
| FR-CITY-02 | 目的地校验失败时，系统应直接返回 400 错误，且不启动智能体、不产生任何模型调用 | P0 | 非法城市请求无智能体执行日志 |
| FR-CITY-03 | 系统应将归一化后的规范城市名用于后续处理 | P0 | "成都市"归一化为"成都"后参与生成 |

#### 3.2.3 行程生成（核心）

| 编号 | 需求描述 | 优先级 | 验证 |
|---|---|---|---|
| FR-GEN-01 | 系统应实现 ReAct 智能体循环：思考（制定工具调用计划）→ 行动（并行执行工具）→ 观察（判断数据充分性），循环次数不超过 3 轮 | P0 | 生成轨迹包含计划、执行、反思、生成四类步骤 |
| FR-GEN-02 | 智能体应基于工具目录动态决策工具调用（知识库检索、天气、地图兴趣点、联网搜索），计划解析失败时使用默认计划兜底 | P0 | 计划阶段输出工具调用列表；解析失败不中断流程 |
| FR-GEN-03 | 系统应支持多工具并行执行，单轮工具执行设置超时限制；单个工具失败不中断整体流程 | P0 | 任一工具异常时其余工具正常执行 |
| FR-GEN-04 | 系统应在数据充分性判定中采用规则短路：已具备兴趣点数据、天气数据及（知识库或搜索）数据之一以上时判定为足够，且不再进行多余模型调用 | P0 | 数据齐全场景下反思阶段无模型调用 |
| FR-GEN-05 | 系统应通过 SSE 流式推送生成过程，事件类型包括：进度、步骤、行程结果、完成、错误；客户端断开时应停止执行并释放资源 | P0 | 前端逐阶段接收事件；断开后后端及时收尾 |
| FR-GEN-06 | 系统应对生成阶段模型调用设置超时保护，超时后返回错误信息，前端不应无限等待 | P0 | 模型慢响应场景返回明确错误提示 |
| FR-GEN-07 | 系统应保障用户额外要求中点名景点的落实：解析点名景点（攻略匹配与地图定向查询）、在提示中明确要求、生成后校验，未落实时在结果中说明原因 | P0 | 点名景点进入行程且信息真实；无法落实时结果包含原因说明 |
| FR-GEN-08 | 系统应校验请求参数合法性：目的地非空、结束日期不早于开始日期、行程天数不超过 31 天、人数不小于 1、预算非负 | P0 | 非法参数返回 400 与明确提示 |

#### 3.2.4 知识库检索

| 编号 | 需求描述 | 优先级 | 验证 |
|---|---|---|---|
| FR-RAG-01 | 系统应支持知识库目录驱动加载：启动时扫描攻略文档目录，从文档标题提取城市名，新增城市无需修改代码 | P0 | 当前支持 10 个城市；新增文档后重启即可识别 |
| FR-RAG-02 | 系统应实现混合检索：向量检索与关键词检索（BM25）结果经 RRF 融合排序；向量检索不可用时自动降级为关键词检索 | P0 | 检索结果包含来源与标题；向量服务不可用时系统仍返回结果 |
| FR-RAG-03 | 系统应支持向量持久化缓存：按攻略片段存储，内容变更后自动重算，避免重复调用向量化服务 | P1 | 攻略未变化时重启复用缓存 |
| FR-RAG-04 | 系统应将攻略文档解析为结构化"攻略卡片"（景点名称、位置、门票、游玩时长、简介、注意事项），供事实回写使用 | P0 | 每个城市解析出不少于 10 张景点卡片 |
| FR-RAG-05 | 系统应对知识库检索结果设置缓存（Redis，6 小时有效期），并支持按偏好标签过滤 | P1 | 相同查询命中缓存 |

#### 3.2.5 数据可信度保障

| 编号 | 需求描述 | 优先级 | 验证 |
|---|---|---|---|
| FR-TRUST-01 | 系统应在行程生成后执行攻略卡片事实回写：景点名称命中攻略卡片时，以卡片中的位置、简介、门票覆盖模型输出，来源标注为"本地攻略" | P0 | 攻略覆盖的景点地址与简介与攻略一致 |
| FR-TRUST-02 | 系统应以地图数据中的真实地址覆盖景点与酒店地址 | P0 | 景点地址为地图返回的真实地址 |
| FR-TRUST-03 | 系统应执行跨天去重：同一景点、同一餐厅在全行程中不得重复出现，重复项以真实候选替换 | P0 | 全行程无重复景点与餐厅 |
| FR-TRUST-04 | 系统应保证餐厅来源真实：餐厅名称须来自地图兴趣点或攻略原文，连锁快餐品牌自动替换为候选池真实餐厅 | P0 | 行程中无黑名单连锁品牌；餐厅来源可追溯 |
| FR-TRUST-05 | 系统应执行恶劣天气替代：预报含雷、暴、雨、雪等恶劣天气字眼时，当日户外景点替换为室内候选，无候选时给出警示 | P0 | 恶劣天气日行程以室内活动为主 |
| FR-TRUST-06 | 系统应执行预算合理性修正：估算总预算与用户预算偏差超过阈值时，仅调整价格字段以匹配预算，且不改变行程结构与项目名称 | P0 | 极端预算输入下总价处于合理区间，系统不崩溃 |
| FR-TRUST-07 | 系统应对住宿费用建模：按住宿档次设定单间价格区间，多人出行按房间数（约等于人数/2）计算 | P1 | 多人行程住宿费用符合房型与档次 |
| FR-TRUST-08 | 系统应防止住宿场所被列入景点列表 | P1 | 景点列表中不包含当日住宿场所 |
| FR-TRUST-09 | 系统应执行景点描述交叉检测：景点描述中出现其他地点名称时，剥离相关语句并提示；对地理区域通名不做误报 | P1 | 无内容错位输出；地理通名不产生误报 |
| FR-TRUST-10 | 系统应为景点、餐厅、交通等项目标注数据来源，供前端展示可信度 | P1 | 每个项目具有来源标签 |
| FR-TRUST-11 | 系统应保证数据来源说明与预算明细一致：来源说明中不得出现模型编造的预算核算内容，预算以系统计算结果为唯一事实来源 | P0 | 来源说明与实际预算明细一致 |

#### 3.2.6 个性化服务

| 编号 | 需求描述 | 优先级 | 验证 |
|---|---|---|---|
| FR-PER-01 | 系统应基于用户已保存的历史行程（不超过最近 5 条）构建用户画像，画像内容包含：已游览城市、最近行程摘要、节奏偏好、住宿档次偏好、餐饮偏好、平均预算 | P0 | 有历史行程的用户生成时日志出现画像注入记录 |
| FR-PER-02 | 系统应将用户画像注入生成过程，并约束：目的地重复时安排新玩法、延续用户偏好、不编造用户经历 | P0 | 同一用户重复目的地的两次行程存在明显区分 |
| FR-PER-03 | 系统应在行程结果中标识本次生成已结合用户历史偏好 | P1 | 结果页可见个性化标识 |
| FR-PER-04 | 系统应保证个性化相关缓存按用户隔离，不同用户的生成结果不得互相串用 | P0 | 用户 A 的个性化结果不会被用户 B 获取 |
| FR-PER-05 | 系统应保证画像构建失败时静默降级，无历史行程用户的行为与未开启个性化时一致 | P0 | 新用户生成过程无异常 |

#### 3.2.7 行程管理

| 编号 | 需求描述 | 优先级 | 验证 |
|---|---|---|---|
| FR-TRIP-01 | 系统应支持保存行程：行程内容与智能体轨迹一并落库，归属当前用户 | P0 | 保存后可在列表查询 |
| FR-TRIP-02 | 系统应支持查询当前用户的行程列表，按创建时间倒序 | P0 | 仅返回当前用户行程 |
| FR-TRIP-03 | 系统应支持查询行程详情，包含智能体轨迹 | P0 | 详情包含轨迹数据 |
| FR-TRIP-04 | 系统应支持删除行程，采用逻辑删除，仅限当前用户 | P0 | 删除后不再出现在列表 |

#### 3.2.8 行程导出

| 编号 | 需求描述 | 优先级 | 验证 |
|---|---|---|---|
| FR-EXP-01 | 系统应支持将行程导出为 Markdown 文档 | P1 | 导出内容完整可读 |
| FR-EXP-02 | 系统应支持将行程导出为 PDF 文档，中文显示正常 | P1 | 导出 PDF 中文正常 |

#### 3.2.9 前端交互

| 编号 | 需求描述 | 优先级 | 验证 |
|---|---|---|---|
| FR-UI-01 | 规划表单应提供日历方式选择开始与结束日期，并满足约束：开始日期不得早于当前日期、结束日期不得早于开始日期、行程天数不超过 7 天 | P0 | 日历禁用规则生效；修改天数联动结束日期 |
| FR-UI-02 | 生成过程页面应实时展示智能体思考轨迹，并支持历史轨迹的逐步回放 | P0 | 生成中逐步展示；历史轨迹可回放 |
| FR-UI-03 | 行程结果页面应展示行程摘要、每日安排、预算明细、天气信息、地图点位与数据来源说明 | P0 | 各区域数据完整展示 |
| FR-UI-04 | 历史行程页面应支持查看、轨迹回放、删除与导出操作 | P1 | 操作完整可用 |
| FR-UI-05 | 系统应实现登录状态保持，未登录访问受保护页面时跳转至登录页 | P0 | 未登录跳转登录 |

### 3.3 性能需求

| 编号 | 需求描述 | 验证 |
|---|---|---|
| PR-01 | 相同请求参数（含用户）在缓存有效期内再次生成时，应直接返回缓存结果 | 二次请求响应时间显著缩短 |
| PR-02 | 行程生成结果缓存有效期为 30 分钟；外部数据缓存有效期为：兴趣点 24 小时、天气 30 分钟、知识库 6 小时 | 缓存到期后自动重新获取 |
| PR-03 | 智能体单轮工具执行应并行进行，并设置超时上限 | 工具阶段耗时可控 |
| PR-04 | 模型生成调用应设置超时上限（主生成 90 秒），超时返回错误 | 超时场景不挂起 |

### 3.4 设计约束

1. 后端采用 Java 语言与 Spring Boot 框架，遵循分层架构；
2. 前端采用 Vue 3 与 TypeScript，组件库使用 Ant Design Vue；
3. 数据库采用 MySQL 8.0，数据库连接经环境变量配置；
4. 密钥与外部服务凭证一律通过环境变量注入，不写入代码仓库。

### 3.5 软件系统属性

#### 3.5.1 可靠性

1. 任一外部服务（缓存、向量库、向量化服务、搜索服务）不可用时，系统应自动降级并继续完成生成流程；
2. 模型调用应具备超时、失败与客户端断连三类兜底处理；
3. 生成过程出现异常时应返回明确错误信息，不产生挂起。

#### 3.5.2 可用性

1. 生成过程应通过流式推送实时反馈进度；
2. 请求参数校验失败、目的地非法、生成失败等场景应返回用户可理解的提示信息。

#### 3.5.3 安全性

1. 用户口令以 BCrypt 哈希存储；
2. 受保护接口基于 JWT 鉴权；
3. 用户标识由服务端注入，防止伪造归属；
4. 输入数据经过合法性校验，防止非法参数。

#### 3.5.4 可维护性

1. 代码遵循分层架构，各层职责单一；
2. 知识库通过文档目录驱动扩展，新增城市无需修改代码；
3. 开发过程中产生的问题与解决方案应形成记录文档，便于追溯。

#### 3.5.5 可移植性

1. 服务端应可在 Windows 与 Linux 环境运行；
2. 数据库与缓存支持通过 Docker Compose 一键编排部署。

### 3.6 其他需求

1. 行程内容基于真实数据生成，价格信息仅供规划参考，不构成消费承诺；
2. 系统对未纳入知识库的旅行目的地仍应提供降级生成能力。

---

## 4. 附录

### 附录 A 数据需求

系统使用 MySQL 数据库，包含 4 张数据表：

| 表名 | 主要字段 | 用途 |
|---|---|---|
| users | id、username（唯一）、password_hash、nickname、deleted | 用户账号信息 |
| trip_record | trip_id（唯一）、destination、itinerary_json（JSON）、user_id、deleted | 行程记录，JSON 保存完整行程 |
| agent_trace | trip_id、step、thought、action、observation、tool_calls（JSON） | 智能体决策轨迹 |
| guide_embedding | source、chunk_title、content_hash、model、dim、vector | 知识库片段向量持久化缓存 |

### 附录 B 系统接口清单

| 方法 | 路径 | 鉴权 | 说明 |
|---|---|---|---|
| POST | /auth/register | 否 | 用户注册 |
| POST | /auth/login | 否 | 用户登录，返回令牌 |
| POST | /trip/generate | 是 | 生成行程（无轨迹） |
| POST | /trip/generate-with-trace | 是 | 生成行程（含轨迹） |
| POST | /trip/generate-stream | 是 | 流式生成行程（SSE） |
| GET | /trip | 是 | 行程列表 |
| GET | /trip/{trip_id} | 是 | 行程详情 |
| POST | /trip/save | 是 | 保存行程 |
| DELETE | /trip/{trip_id} | 是 | 删除行程 |
| GET | /export/{trip_id}/markdown | 是 | Markdown 导出 |
| GET | /export/{trip_id}/pdf | 是 | PDF 导出 |
| GET | /weather/forecast | 是 | 天气查询 |
