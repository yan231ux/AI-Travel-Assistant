# 系统全量问题与 AI 审核数据运营升级方案

> 审查日期：2026-09-10  
> 适用对象：Workbuddy 后续实现  
> 审查范围：普通用户端、管理员后台、社区内容、城市攻略、RAG 同步、推荐系统、个性化、统计监控和前端构建

---

## 一、总体结论

当前系统已经从单纯的 AI 行程规划器发展为包含以下模块的平台雏形：

- AI 行程规划；
- 用户画像和个性化推荐；
- 景点数据底座；
- 城市专题；
- 用户帖子、评论、点赞、收藏和举报；
- 独立管理员后台；
- 帖子和攻略审核；
- 城市攻略版本管理；
- RAG 攻略索引和失败回滚；
- 推荐 A/B 实验和人工干预；
- 推荐曝光和反馈监控；
- 审计日志。

本轮巡检结论：

> 基础功能和管理后台骨架已经比较完整，但“AI 审核”和“数据驱动运营”还没有真正形成闭环。当前后台能查看部分统计，但还不能回答“哪个景点真正被用户规划最多、哪个城市增长最快、哪些内容应该推荐到首页”。

当前优先级建议：

```text
P0：审核安全、统计口径、RAG/数据库一致性、后台 fail-soft 误导
P1：AI 审核、人机协同审核、景点规划采用量统计、图表看板
P2：热门内容自动反哺首页、运营策略自动化、预测和异常检测
```

---

## 二、本轮确认已经做好的部分

### 2.1 管理后台已经独立

当前已经存在独立的：

- `AdminLayout`；
- `/admin` 路由；
- 管理员路由守卫；
- 帖子审核详情页；
- 举报详情页；
- 攻略管理和编辑页；
- 景点治理页；
- 用户治理页；
- 推荐运营页；
- 审计页。

旧的 `/moderation` 已经兼容重定向到管理员审核页面，方向正确。

### 2.2 攻略和 RAG 版本链路已经明显改善

当前已经实现：

- 数据库攻略版本作为线上内容；
- `current_revision_id` 和 `published_revision_id`；
- 攻略发布后的 RAG 索引任务；
- RAG 索引成功后切换线上版本；
- 索引失败时保留旧版本；
- 索引执行前校验攻略状态和 revision；
- 旧任务过期后标记为 `SUPERSEDED`；
- Markdown 幂等导入；
- 攻略下线、恢复、归档和回滚。

这部分已经不再是简单的“管理员修改数据库、RAG 继续读取旧 Markdown”。

### 2.3 验证结果

当前已确认：

- 后端全量测试通过；
- 前端 `npm run build` 通过；
- 管理端页面使用动态路由分包；
- RAG 运行时应用数据库攻略版本的能力已存在；
- 新增的管理员、攻略、景点、推荐干预测试已加入。

但构建和单元测试通过，不等于真实生产数据口径已经正确，尤其是统计和异步任务仍需要集成测试。

---

# 三、全系统问题清单

## P0-1 管理后台首页统计还不够完整，且 fail-soft 可能掩盖故障

### 当前问题

`AdminDashboardService` 对部分统计异常采用：

```text
查询失败 -> 返回 0
```

这会让管理员看到：

```text
景点数量 0
用户数量 0
待审内容 0
```

但真实情况可能是数据库查询失败、表结构未升级、权限异常或连接异常。

### 风险

管理员可能误以为系统没有数据，而不是系统统计失败。

### 建议

每个统计模块返回：

```json
{
  "value": 120,
  "status": "OK",
  "error": null,
  "generated_at": "2026-09-10 12:00:00"
}
```

失败时：

```json
{
  "value": null,
  "status": "ERROR",
  "error": "查询失败，请检查数据库连接"
}
```

前端显示：

```text
—
数据暂时不可用
重试
```

不要把系统故障伪装成业务上的 0。

---

## P0-2 推荐曝光量不能代表“景点被规划的人数”

当前 `FeedMonitorService` 主要统计：

- 推荐曝光；
- 推荐命中率；
- 收藏；
- 不感兴趣；
- A/B 变体。

这些数据不能回答：

```text
哪个景点被用户加入了行程？
哪个景点真正出现在生成结果里？
哪个景点被用户保存或重复规划最多？
```

推荐曝光多，不代表被规划多。例如一个景点可能大量曝光，但用户从不把它加入行程。

### 必须区分的指标

```text
曝光量：推荐卡片返回/进入可视区域
点击量：用户打开景点详情
收藏量：用户收藏景点
加入行程意向量：用户点击“加入行程”
规划采用量：生成结果中真正出现该景点
保存行程量：用户保存包含该景点的行程
复访/重复规划量：用户后续再次使用该景点
```

老师说的“哪个景点规划的人多”，建议使用：

> **规划采用用户数**：去重用户数，统计指定时间内至少一次生成行程结果中出现该景点的用户数量。

同时显示：

- 规划采用次数；
- 规划采用用户数；
- 保存行程次数；
- 加入行程点击次数；
- 收藏次数；
- 采用率 = 规划采用用户数 / 景点详情或推荐曝光用户数。

不能用推荐曝光量直接替代规划人数。

---

## P0-3 规划结果中的景点采用事件还不够标准化

目前行程生成、推荐日志和行为反馈已经存在，但需要统一一个明确的“行程采用事件”。

建议新增事件表：

```text
trip_item_event
- id
- user_id
- trip_id
- spot_id
- poi_id
- spot_name_snapshot
- city
- source_type       # RECOMMENDATION / USER_REQUEST / LLM / GUIDE
- event_type        # GENERATED / SAVED / REMOVED / REPLACED
- created_at
```

至少在以下时间写入：

1. 行程生成完成并通过校验；
2. 结果中确认景点真实存在；
3. 用户保存行程；
4. 用户删除或替换景点。

统计时优先使用 `spot_id`，其次使用 `poi_id`，最后才使用规范化名称。不能只依赖景点名称，否则同名景点会混淆。

---

## P0-4 AI 审核不能直接替代人工审核

AI 审核适合做：

- 初筛；
- 风险分类；
- 违规片段定位；
- 相似内容识别；
- 信息完整性检查；
- 外部事实风险提示。

AI 不应该直接无条件：

- 永久封禁用户；
- 删除内容；
- 直接发布攻略；
- 修改用户正文；
- 以模型判断作为唯一证据。

必须采用：

```text
规则审核 + AI 初筛 + 人工复核 + 审计记录
```

---

# 四、AI 内容审核完整方案

## 4.1 审核对象

AI 审核覆盖四类内容：

```text
用户帖子
用户评论
管理员城市攻略
景点简介和外部导入内容
```

行程生成结果不直接进入社区审核队列，但可以进入“事实风险提示”：

- 门票价格可能过期；
- 营业时间不确定；
- 景点地址缺失；
- 景点和城市不匹配；
- 内容与用户需求冲突。

## 4.2 审核流水线

```text
内容创建/编辑
    -> 同步规则检查
    -> 生成内容 fingerprint
    -> AI 审核任务入队
    -> 规则与 AI 并行分析
    -> 聚合风险等级
    -> 自动放行 / 人工复核 / 自动拦截
    -> 人工最终决策
    -> 发布或拒绝
    -> 审计记录
```

## 4.3 规则审核层

规则层必须先于 AI，便宜、确定、可解释：

- 联系方式和导流信息；
- 手机号、微信、QQ、二维码描述；
- 明显广告词；
- 违法、暴力、色情和仇恨内容；
- 正文长度；
- 标题和正文重复；
- 内容与城市不匹配；
- 图片格式、大小和魔数；
- 外部 URL 域名；
- 重复帖子和重复攻略。

规则命中直接记录：

```text
rule_code
rule_name
matched_text
severity
```

## 4.4 AI 审核任务表

建议新增：

```text
content_moderation_task
- id
- target_type              # POST / COMMENT / GUIDE / SPOT
- target_id
- revision_id              # 攻略必须绑定版本
- content_hash
- task_type                # SAFETY / QUALITY / FACT_CHECK / DUPLICATE
- status                   # PENDING / RUNNING / PASSED / REVIEW / REJECTED / FAILED
- risk_level               # LOW / MEDIUM / HIGH / CRITICAL
- risk_score
- model_name
- prompt_version
- result_json
- matched_spans_json
- error_message
- retry_count
- created_at
- finished_at
```

必须绑定 `content_hash` 或 `revision_id`，防止旧 AI 结果覆盖新版本内容。

## 4.5 AI 输出结构

不要让 AI 返回一段无法解析的自然语言，要求结构化 JSON：

```json
{
  "decision": "PASS|REVIEW|REJECT",
  "risk_level": "LOW|MEDIUM|HIGH|CRITICAL",
  "risk_score": 0.82,
  "categories": [
    {
      "code": "ADVERTISING",
      "confidence": 0.94,
      "evidence": "添加微信获取低价门票"
    }
  ],
  "quality": {
    "score": 78,
    "missing": ["缺少具体交通信息"]
  },
  "fact_risks": [
    {
      "field": "ticket_price",
      "text": "门票只要 10 元",
      "reason": "价格可能过期"
    }
  ],
  "suggestion": "建议补充信息来源并删除导流内容"
}
```

## 4.6 自动决策阈值

建议初始阈值：

```text
规则命中高危：直接进入人工复核或拦截
AI risk_score >= 0.90：HIGH，必须人工复核
0.65 <= risk_score < 0.90：MEDIUM，进入审核队列
risk_score < 0.65 且规则通过：可自动通过或抽样复核
AI 调用失败：不能自动放行高风险内容，进入待审
```

AI 审核结果是建议，不是最终事实。管理员可覆盖 AI 结论，但必须填写原因。

## 4.7 AI 审核后台页面

路径：

```text
/admin/content/ai-review
/admin/content/ai-review/:id
```

列表展示：

- 内容类型；
- 标题；
- 城市；
- 风险等级；
- 风险分；
- AI 决策；
- 规则命中数；
- 是否需要事实核验；
- AI 模型和版本；
- 创建时间；
- 当前处理人。

详情页展示：

- 原文；
- AI 命中的文本高亮；
- 规则命中；
- AI 风险解释；
- 事实风险；
- 相似内容；
- 历史审核；
- 最终人工决定。

---

# 五、后台数据看板方案

## 5.1 管理后台首页

现有后台首页只有 KPI 和最近操作，建议增加四个区域：

```text
顶部：核心 KPI
左侧：内容审核漏斗
中间：景点/城市热度图表
右侧：系统风险和索引状态
底部：最近操作和趋势
```

## 5.2 核心 KPI

```text
待审核帖子
待审核攻略
待处理举报
AI 高风险待复核
近 7 天新增行程
近 7 天规划采用景点数
近 7 天活跃用户
RAG 索引失败任务
```

每个 KPI 必须支持：

- 时间窗口；
- 点击跳转对应列表；
- 数据更新时间；
- 错误状态显示；
- 同比或环比。

## 5.3 必须增加的图表

### 图表一：景点规划采用排行

类型：横向柱状图。

维度：

- 景点名称；
- 城市；
- 规划采用用户数；
- 规划采用次数。

筛选：

- 最近 1 天；
- 最近 7 天；
- 最近 30 天；
- 城市；
- 是否来自推荐；
- 是否被用户明确点名。

用途：回答“哪个景点规划的人多”。

### 图表二：城市热度排行

类型：柱状图或地图式列表。

指标：

- 规划生成次数；
- 保存行程次数；
- 活跃用户数；
- 景点采用用户数；
- 社区攻略发布数。

### 图表三：内容审核漏斗

类型：漏斗图或堆叠柱状图。

```text
新增内容
 -> 规则拦截
 -> AI 通过
 -> AI 待复核
 -> 人工通过
 -> 发布
 -> 下架/拒绝
```

### 图表四：内容风险构成

类型：饼图或环形图。

分类：

- 广告导流；
- 虚假信息；
- 低质量；
- 重复内容；
- 敏感内容；
- 图片问题；
- 其他。

饼图只适合展示构成，不适合展示趋势。趋势使用折线图。

### 图表五：推荐效果趋势

类型：折线图。

指标：

- 推荐曝光；
- 详情点击；
- 收藏率；
- 加入行程率；
- 规划采用率；
- 不感兴趣率。

### 图表六：RAG 索引状态

类型：堆叠柱状图或状态卡。

```text
READY
INDEXING
FAILED
SUPERSEDED
NOT_INDEXED
```

点击失败项进入索引任务详情和重试操作。

## 5.4 技术实现建议

如果项目不想增加大型图表依赖，建议接入轻量级 `echarts`：

- `BarChart`：景点、城市排行；
- `LineChart`：趋势；
- `PieChart`：内容风险构成；
- `FunnelChart`：审核漏斗。

图表组件必须：

- 只在管理员页面懒加载；
- 没有数据时显示空状态；
- 数据错误时显示错误状态；
- 支持窗口大小变化；
- 支持 tooltip 和数据表格切换；
- 不只展示图形，必须能查看具体数值。

---

# 六、热门景点反哺首页方案

## 6.1 不要直接把“规划最多”写入首页

首页展示内容必须区分：

```text
热门规划：大家最近规划较多
个性化推荐：根据当前用户画像
城市精选：运营人工精选
攻略收录：本地攻略已有内容
```

不能把全站热门伪装成“适合你”。

## 6.2 推荐的首页模块

新增首页模块：

```text
大家最近在规划
```

卡片展示：

- 景点图片；
- 景点名称；
- 城市；
- “近 7 天有 128 人规划过”；
- “来自成都旅行计划”；
- 收藏按钮；
- 查看详情；
- 加入行程。

如果用户有画像，可以同时展示：

```text
大家最近在规划 · 也符合你的自然风景偏好
```

如果没有命中画像，只显示社会热度，不显示匹配度。

## 6.3 热门分计算

初版不要只按总次数排序，建议使用时间衰减：

```text
hot_score =
  0.40 × normalized_planning_users
+ 0.25 × normalized_saved_trips
+ 0.15 × normalized_favorites
+ 0.10 × normalized_detail_clicks
- 0.10 × normalized_dislikes
```

加时间衰减：

```text
decayed_score = hot_score × exp(-age_hours / half_life_hours)
```

建议半衰期：

- 景点热度：72 小时；
- 城市热度：7 天；
- 社区帖子热度：48 小时。

必须按城市或类别归一化，避免大城市因为数据量大长期霸榜。

## 6.4 首页数据接口

建议新增：

```text
GET /home/trending-spots?city=&days=7&limit=8
```

返回：

```json
{
  "items": [
    {
      "spot_id": "spot_chengdu_xxx",
      "name": "大熊猫繁育研究基地",
      "city": "成都",
      "planning_users": 128,
      "planning_count": 173,
      "saved_trip_count": 64,
      "favorite_count": 91,
      "trend": "UP",
      "data_quality": "GUIDE_MATCHED",
      "matched_preferences": ["自然风景"]
    }
  ],
  "window_days": 7,
  "generated_at": "2026-09-10 12:00:00"
}
```

后台接口：

```text
GET /admin/analytics/trending-spots
GET /admin/analytics/cities
GET /admin/analytics/content-risk
GET /admin/analytics/overview
```

## 6.5 首页反哺安全规则

热门景点进入首页前必须满足：

- 景点状态为 `ONLINE`；
- 没有 `BLACKLIST` 干预；
- 不是 `NON_SPOT`、`CLOSED`、`OUTDATED`；
- 至少有有效名称和城市；
- 图片失效时有占位；
- 数据质量过低时显示“地图数据”，不能伪装为已核验；
- 统计窗口内至少有最小样本量；
- 不展示个人隐私信息。

建议设置：

```text
规划用户数 < 3：不显示具体人数，只显示“近期有人规划”
```

避免小样本泄露用户行为。

---

# 七、统计数据模型和埋点改造

## 7.1 事件统一模型

建议新增通用事件表：

```text
travel_event
- id
- user_id
- session_id
- event_type
- item_type
- item_id
- item_name_snapshot
- city
- trip_id
- source
- metadata_json
- created_at
```

事件类型：

```text
SPOT_EXPOSED
SPOT_CLICKED
SPOT_FAVORITED
SPOT_UNFAVORITED
SPOT_ADD_TO_PLAN
SPOT_INCLUDED_IN_TRIP
TRIP_GENERATED
TRIP_SAVED
TRIP_DELETED
POST_EXPOSED
POST_CLICKED
GUIDE_VIEWED
```

现有 `user_behavior` 可以继续保留做画像反馈，但统计分析应使用事件表，避免把画像行为表强行当作产品分析表。

## 7.2 去重规则

必须区分：

- 次数；
- 用户数；
- 行程数；
- 会话数。

例如：

```text
planning_count = 事件总数
planning_users = COUNT(DISTINCT user_id)
planning_trips = COUNT(DISTINCT trip_id)
```

同一用户同一行程重复刷新结果不能重复统计。

## 7.3 数据延迟

推荐采用：

- 事件写入数据库或消息队列；
- 后台定时聚合小时/天级统计；
- 首页读取预聚合结果；
- 管理员查看实时或近实时统计。

初版可以直接 SQL 聚合，但必须限制时间窗口和分页，不能每次首页打开都扫描全部行为数据。

建议新增：

```text
spot_trending_daily
city_trending_daily
content_moderation_daily
```

---

# 八、AI 审核与后台图表的实施顺序

## 第一阶段：先修统计基础和现有 Bug

1. 增加 `travel_event` 或 `trip_item_event`；
2. 在行程生成完成、保存行程、加入行程、收藏景点处写事件；
3. 区分曝光、点击、加入行程、规划采用和保存行程；
4. 修复后台统计失败返回 0 的问题；
5. 为统计接口增加时间窗口和数据更新时间；
6. 修复 RAG 索引任务并发、恢复和回滚测试；
7. 确认 `BLACKLIST/DEMOTE/PIN` 在推荐流中真实生效；
8. 补充数据库集成测试。

## 第二阶段：AI 审核初筛

1. 新增审核任务表；
2. 接入现有 LLM 客户端，不新造一套 AI 调用链；
3. 规则检查和 AI 检查并行；
4. 结构化保存 AI 输出；
5. 建立风险等级和阈值；
6. AI 失败自动进入人工审核；
7. 管理员可覆盖 AI 结论；
8. 所有决策写审计日志。

## 第三阶段：后台数据看板

1. 接入 ECharts；
2. 添加景点规划采用排行；
3. 添加城市热度排行；
4. 添加审核漏斗；
5. 添加风险构成饼图；
6. 添加推荐效果趋势；
7. 添加 RAG 索引状态；
8. 图表支持空、错、加载和表格模式。

## 第四阶段：首页热度反哺

1. 生成每日/小时景点热度聚合；
2. 新增热门景点接口；
3. 首页增加“大家最近在规划”；
4. 热门和个性化推荐分开展示；
5. 接入景点状态和人工黑名单过滤；
6. 加入最小样本保护；
7. 统计热门模块的点击和转化。

## 第五阶段：运营自动化

1. 热门景点自动候选；
2. 管理员人工确认置顶；
3. 运营干预有效期；
4. 异常热度检测；
5. AI 生成运营摘要；
6. AI 只生成建议，不直接改变线上推荐；
7. 每次运营干预可回滚、可审计。

---

# 九、需要补充的测试

## AI 审核

```text
规则命中高危内容 -> 进入人工复核
AI 返回 HIGH -> 不能自动发布
AI 返回 PASS 且规则通过 -> 可以自动放行
AI 调用失败 -> 进入人工复核，不自动发布
旧 revision 的 AI 结果 -> 不能覆盖新 revision
管理员覆盖 AI 结论 -> 必须记录原因和审计
重复提交同一 content_hash -> 不重复创建审核任务
```

## 统计

```text
同一用户同一行程重复刷新 -> 只算一次采用
同一景点不同用户规划 -> planning_users 正确去重
加入行程但未生成 -> 只计 ADD_TO_PLAN，不计 INCLUDED_IN_TRIP
生成但未保存 -> 计 GENERATED，不计 SAVED
删除景点 -> 不影响历史生成采用量，但影响当前保留统计
不同城市同名景点 -> 以 spot_id 区分
```

## 热门首页

```text
BLACKLIST 景点 -> 不进入热门模块
OFFLINE 景点 -> 不进入热门模块
规划数不足最小样本 -> 不显示具体人数
热门景点无图片 -> 使用稳定占位
热门景点没有个性化命中 -> 不显示匹配度
热门排序有时间衰减 -> 新数据可以超过长期旧热度
```

## RAG

```text
新版本索引失败 -> 旧版本继续检索
旧任务晚完成 -> 不能覆盖新版本
攻略下线 -> RAG 不再检索
攻略恢复 -> 自动重新创建索引任务
回滚版本 -> RAG 与 published_revision_id 一致
同一 revision 重试 -> 不产生重复线上片段
```

---

# 十、可以直接交给 Workbuddy 的执行提示词

请基于 `系统全量问题与AI审核数据运营升级方案.md`，先对现有系统进行一次回归修复，再新增 AI 审核、后台数据看板和热门景点首页反哺能力。

必须遵守：

1. 先修复 P0 数据和状态问题，再增加图表和 AI 功能；
2. “哪个景点规划的人多”必须基于行程采用事件或 `trip_item_event`，不能用推荐曝光量冒充；
3. 必须区分曝光、点击、收藏、加入行程、生成采用和保存行程；
4. 统计至少支持用户数、次数、行程数三种口径；
5. AI 审核只能作为初筛和风险提示，不能无条件替代人工；
6. AI 审核结果必须结构化保存，并绑定 `content_hash` 或 `revision_id`；
7. AI 调用失败时进入人工复核，不能默认放行高风险内容；
8. 后台首页增加景点规划排行、城市热度、审核漏斗、风险构成、推荐效果趋势和 RAG 状态；
9. 图表必须支持加载、空数据、错误、更新时间和表格查看；
10. 首页新增“大家最近在规划”模块，但必须与“为你推荐”分开，不能把热门伪装成个性化；
11. 热门景点必须过滤下线、黑名单、关闭和异常 POI，并设置最小样本保护；
12. 保留现有管理员权限校验、RAG 版本机制、审计日志和普通用户功能；
13. 不要直接扫描全量行为表作为首页实时接口，至少增加时间窗口、索引和预聚合策略；
14. 完成后运行后端全量测试和 `npm run build`，补充服务级测试和关键数据库集成测试；
15. 汇报新增表、接口、事件口径、AI 模型版本、失败重试策略和图表数据来源。

禁止：

- 不要把 AI 输出直接写入线上公开内容；
- 不要把模型自然语言当作唯一审核依据；
- 不要把曝光数叫作规划人数；
- 不要把热门景点显示成“适合你”；
- 不要把图表做成没有数据来源的静态演示；
- 不要在 AI 调用失败时静默通过高风险内容；
- 不要用 fail-soft 的 0 掩盖数据库或统计接口故障。

---

# 十一、最终验收标准

## 系统可靠性

- RAG 发布、失败、恢复、回滚和旧任务竞态均有测试；
- 数据库、RAG 和管理后台显示的当前版本一致；
- 统计查询失败时显示“数据不可用”，而不是 0；
- 推荐人工干预真实影响推荐流，且有时间窗口和审计。

## AI 审核

- 帖子、评论、攻略和景点内容都能进入审核流水线；
- 高风险内容不会因为 AI 失败而自动公开；
- AI 结果能定位风险片段；
- 管理员能覆盖 AI 结论并填写原因；
- 旧内容的 AI 结果不会覆盖新版本。

## 数据看板

- 后台有景点规划采用排行；
- 后台有城市热度排行；
- 后台有审核漏斗和风险构成；
- 后台有推荐效果趋势；
- 后台有 RAG 索引状态；
- 所有图表能查看具体数据和更新时间。

## 首页反哺

- 首页有“大家最近在规划”模块；
- 热门景点使用真实规划采用数据；
- 热门和个性化推荐视觉和文案区分；
- 下线/黑名单/异常景点不会进入首页；
- 小样本数据不会暴露具体用户行为；
- 用户点击热门景点可以进入详情和加入行程。

---

## 十二、总结

这套系统下一阶段的重点不是继续增加孤立功能，而是建立两个真正的数据闭环：

### 内容闭环

```text
用户/管理员创建内容
  -> 规则检查
  -> AI 初筛
  -> 人工审核
  -> 发布
  -> RAG 索引
  -> 用户使用
  -> 举报和反馈
  -> 内容质量复盘
```

### 数据运营闭环

```text
用户曝光和点击
  -> 收藏/加入行程
  -> 行程生成采用
  -> 保存行程
  -> 统计景点和城市热度
  -> 管理员看板分析
  -> 热门内容进入首页
  -> 用户继续使用和反馈
```

最关键的三条原则：

```text
AI 是审核助手，不是最终裁判
曝光量不是规划人数
热门推荐不是个性化推荐
```

完成这轮改造后，系统才能从“功能比较丰富的旅行应用”进一步变成“能自我积累数据、辅助内容治理、支持运营决策的旅行平台”。
