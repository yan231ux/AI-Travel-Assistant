# 个性化推荐与“加入行程”问题排查报告

> 排查日期：2026-09-08  
> 排查范围：推荐城市/景点、匹配度展示、推荐理由、画像降级、反馈闭环、城市筛选、加入行程、规划页参数接收  
> 排查方式：前端源码检查、后端推荐算法和 DTO 检查、行程生成链路检查、现有测试覆盖检查

## 一、总体结论

当前个性化推荐的后端基础已经比较完整：

- 景点推荐使用统一评分器；
- 推荐排序和推荐日志基本共用评分口径；
- 有画像和无画像会区分处理；
- 收藏和“不感兴趣”可以影响后续画像；
- 推荐曝光已经有 `feed_trace_id` 幂等设计；
- 城市数据支持数量不足或超过 7 天后重新同步；
- 行程生成链路已经支持从 `special_notes` 中解析用户点名景点。

但是，当前最明显的问题集中在“后端实际含义”和“前端展示文案”不一致：

> 推荐卡片展示的“与你的偏好匹配度”并不是纯粹的偏好匹配度，而是一个包含基础分、新颖性惩罚和回避规则的综合排序分。

此外：

> “加入行程”目前只把城市写入规划页，没有把用户点击的具体景点传入行程生成上下文。

因此当前系统可以演示，但如果作为正式产品，建议优先修复以下三项：

1. 禁止把综合排序分直接显示为“与你的偏好匹配度”；
2. 只有真正发生画像命中时才显示匹配度和个性化文案；
3. “加入行程”必须携带具体景点，并让生成器明确把它作为用户指定地点处理。

---

## 二、问题等级

- **P0：明显错误或会误导用户，建议立即修复**；
- **P1：核心交互不完整或推荐语义不准确，建议本轮修复**；
- **P2：体验、统计和长期维护问题，可随后优化**。

---

# 三、P0 问题

## P0-1 匹配度百分比使用了错误的分数含义

### 相关位置

- 前端：[frontend/src/components/SpotCard.vue](frontend/src/components/SpotCard.vue)
- 后端：[backend/src/main/java/com/yuntu/tripplanner/service/PersonalizedScoreCalculator.java](backend/src/main/java/com/yuntu/tripplanner/service/PersonalizedScoreCalculator.java)
- 后端：[backend/src/main/java/com/yuntu/tripplanner/service/RecommendationFeedService.java](backend/src/main/java/com/yuntu/tripplanner/service/RecommendationFeedService.java)

### 当前实现

前端当前逻辑类似：

```ts
if (!props.item.recommend_reason || typeof props.item.score !== "number") return null;
return Math.round(Math.min(1, Math.max(0, props.item.score)) * 100);
```

然后展示：

```text
与你的偏好匹配度 50%
```

但是后端 `score` 来自：

```text
finalScore = clamp01(0.5 + (boost - visitedPenalty) * 0.6)
```

也就是说，`score` 是综合排序分，不是纯粹的“偏好匹配度”。即使没有命中任何偏好，候选也可能得到基础分 `0.5`，最终前端显示：

```text
与你的偏好匹配度 50%
```

这会造成明显误导。

### 具体错误场景

#### 场景 1：没有画像

后端会返回：

```text
personalized = false
score = 0.5 或其他综合分
recommend_reason = 本地攻略收录的真实景点 / 高德热门景点
```

前端因为 `recommend_reason` 不为空，仍然可能显示：

```text
与你的偏好匹配度 50%
```

但用户其实没有画像匹配。

#### 场景 2：有画像但当前景点没有命中

后端会返回兜底理由：

```text
本地攻略收录的真实景点
```

或者：

```text
你曾去过这里，本次为你换点新地方
```

前端仍然只判断理由和分数是否存在，依然可能显示匹配度。

#### 场景 3：攻略优先或最新排序

用户切换到“攻略优先”或“最新”后，卡片仍可能沿用 `score` 展示匹配度，但这个页面并不是个性化排序。

### 建议修复方案

#### 推荐方案：后端拆分字段

不要让前端猜 `score` 的含义，新增明确字段：

```java
@JsonProperty("personalized")
private Boolean personalized;

@JsonProperty("match_score")
private Double matchScore;

@JsonProperty("matched_preferences")
private List<String> matchedPreferences;
```

字段语义建议：

- `score`：仅作为内部排序分或调试字段，不直接展示；
- `personalized`：本条是否由真实个性化规则命中；
- `match_score`：只表示偏好命中程度，范围 0 到 1；
- `matched_preferences`：实际命中的偏好标签；
- `recommend_reason`：面向用户的解释文案。

前端只在以下条件同时满足时展示百分比：

```text
feed.personalized === true
且 item.personalized === true
且 item.match_score != null
且 item.matched_preferences 非空
```

#### 最小修复方案：暂时禁止错误百分比

如果本轮不想改后端 DTO，至少把前端条件改成：

```ts
const matchPercent = computed(() => {
  if (!props.item.personalized) return null;
  if (!props.item.matched_preferences?.length) return null;
  if (typeof props.item.match_score !== "number") return null;
  return Math.round(Math.min(1, Math.max(0, props.item.match_score)) * 100);
});
```

在无法新增字段前，不建议继续把现有 `score` 直接包装成匹配度百分比。

### 必须补充的测试

```text
无画像 + 有兜底推荐理由 -> 不显示匹配度
有画像但没有命中当前景点 -> 不显示匹配度
命中“自然风景” -> 显示真正的匹配度和命中标签
切换“攻略优先” -> 不显示个性化匹配度
切换“最新” -> 不显示个性化匹配度
综合排序分为 0.5 -> 不能自动显示“匹配度 50%”
```

---

## P0-2 “加入行程”丢失了具体景点

### 相关位置

- [frontend/src/components/SpotCard.vue](frontend/src/components/SpotCard.vue)
- [frontend/src/views/SpotDetail.vue](frontend/src/views/SpotDetail.vue)
- [frontend/src/views/PlannerView.vue](frontend/src/views/PlannerView.vue)
- [frontend/src/types/index.ts](frontend/src/types/index.ts)
- [backend/src/main/java/com/yuntu/tripplanner/model/TripRequest.java](backend/src/main/java/com/yuntu/tripplanner/model/TripRequest.java)
- [backend/src/main/java/com/yuntu/tripplanner/agent/TravelAgent.java](backend/src/main/java/com/yuntu/tripplanner/agent/TravelAgent.java)

### 当前行为

景点卡片和景点详情页都只有：

```ts
router.push({ name: "plan", query: { city: item.city } });
```

规划页只接收：

```ts
route.query.city
```

最终效果是：

```text
用户点击“加入行程”
    -> 跳到规划页
    -> 目的地填入城市
    -> 具体景点完全丢失
```

用户点击的是“加入行程”，合理预期应该是：

```text
这个景点会被加入到接下来的行程规划中
```

而不是：

```text
只是帮我填了一个城市
```

### 现有后端能力

当前后端其实已经有一条可复用的能力链：

- `TripRequest.specialNotes` 支持特殊要求；
- `TravelAgent.collectRequestedSpots()` 会从特殊需求文本中解析点名景点；
- `CollectedData.requestedSpots` 会把点名景点放入生成摘要；
- 生成提示词会要求模型必须安排用户点名景点。

因此这是一个前后端没有接上的问题，不需要重新发明整套能力。

### 推荐修复方案

#### 方案 A：最小改动，使用 query 传递景点

点击景点卡时传递：

```ts
router.push({
  name: "plan",
  query: {
    city: item.city,
    spot: item.name,
    spot_id: item.spot_id,
    poi_id: item.poi_id,
  },
});
```

详情页同样传递：

```ts
router.push({
  name: "plan",
  query: {
    city: detail.city,
    spot: detail.name,
    spot_id: detail.spot_id,
    poi_id: detail.poi_id,
  },
});
```

规划页接收后：

1. 目的地填入城市；
2. “额外要求”自动增加一句：`请务必安排：景点名称`；
3. 页面上显示一个可删除的“已加入景点”标签；
4. 用户可以继续填写其他要求；
5. 提交时将合并后的内容放入 `special_notes`；
6. 不要覆盖用户原本已经填写的备注；
7. 同一个景点重复进入时要去重。

例如：

```text
目的地：三亚
已加入景点：三亚湾
额外要求：请务必安排：三亚湾
```

这是当前最适合本项目的第一版方案，因为后端已经能识别 `special_notes` 中的点名景点。

#### 方案 B：长期方案，增加显式 requestedSpots 字段

前端请求类型增加：

```ts
requested_spots?: Array<{
  name: string;
  spot_id?: string;
  poi_id?: string;
}>;
```

后端 `TripRequest` 增加同名字段，生成器直接把它们加入 `CollectedData.requestedSpots`，不依赖自然语言解析。

优点：

- 不会因为文本改写而丢失景点；
- 可以用 `spot_id` / `poi_id` 精确关联；
- 可以在生成结果中校验是否真的安排；
- 可以支持多个“加入行程”的景点。

建议先做方案 A，后续再升级方案 B。

### 交互文案建议

按钮可以保留“加入行程”，但点击后规划页必须出现明确反馈：

```text
已加入：三亚湾
生成行程时会优先安排该景点
```

如果只想进入规划页而不强制安排，应把按钮文案改成：

```text
用这个城市规划
```

不能让“加入行程”实际只做“填入城市”。

### 必须补充的测试

```text
景点卡点击加入行程 -> 城市和景点都进入规划页
景点详情点击加入行程 -> 城市和景点都进入规划页
已有 special_notes -> 不被覆盖，追加点名景点
同一个景点重复点击 -> 不重复追加
提交后 special_notes 包含点名景点
TravelAgent 能识别并安排该景点
```

---

# 四、P1 问题

## P1-1 推荐流的 `personalized` 只按 travel_style 判断

### 相关位置

- [backend/src/main/java/com/yuntu/tripplanner/service/RecommendationFeedService.java](backend/src/main/java/com/yuntu/tripplanner/service/RecommendationFeedService.java)
- [backend/src/main/java/com/yuntu/tripplanner/service/PersonalizedScoreCalculator.java](backend/src/main/java/com/yuntu/tripplanner/service/PersonalizedScoreCalculator.java)

当前景点推荐判断是否个性化，主要看：

```text
是否存在达到阈值的 travel_style 画像
```

这意味着：

- 用户只有城市偏好时，景点流可能不算个性化；
- 用户只有美食偏好时，景点流不会命中，因为景点当前只按 travel_style 计算；
- 用户有节奏、住宿等画像时，景点卡片也不会体现这些画像。

这不一定是代码 Bug，但产品上需要明确：

```text
当前景点推荐到底只个性化“景点类型”，还是要综合城市、节奏、预算、距离等因素？
```

### 建议

第一版可以明确写成：

> 景点推荐目前主要依据旅行风格和景点类型个性化；饮食偏好主要影响行程中的餐厅推荐。

前端不要因为用户拥有任意画像就说“已按你的画像排序”。只有实际参与本条流排序的画像域才应该展示。

长期可以增加：

- 城市偏好加权；
- 预算和门票区间；
- 拥挤度、距离和节奏；
- 历史行程的新颖性；
- 收藏和不感兴趣的类型反馈。

---

## P1-2 推荐理由中的“热门”与实际排序仍可能不一致

后端无画像时的推荐理由可能是：

```text
高德热门景点
```

但当前排序主要是：

```text
GUIDE_MATCHED / VERIFIED 数据质量优先
```

它不是基于真实浏览量、收藏量或行程提及量的热门排序。

### 建议

把无画像兜底文案改成：

```text
城市精选
```

或者：

```text
本地攻略收录的真实景点
```

不要把“数据质量高”表达成“全站热门”。

如果未来要使用“热门”，需要增加并真正使用：

- 浏览量；
- 收藏量；
- 行程被采用次数；
- 最近时间衰减；
- 城市内归一化热度分。

---

## P1-3 “最新”排序的语义需要确认

推荐流查询初始按 `updated_at` 倒序，`latest` 使用保序，因此当前“最新”更接近：

```text
最近同步或更新的景点
```

并不一定是：

```text
最近新增的景点
```

如果旧 POI 每 7 天刷新一次，旧景点会因为重新同步而重新排到前面，用户会误以为它是新收录的。

### 建议

拆分两个时间字段：

- `created_at`：首次进入景点底座的时间；
- `last_synced_at`：最近一次外部数据同步时间。

“最新”使用 `created_at`，而数据刷新使用 `last_synced_at`。

如果暂时不改数据库，把排序 Tab 文案改成：

```text
最近更新
```

---

## P1-4 城市 query 对非热门城市不够一致

### 相关位置

- [frontend/src/views/Recommendations.vue](frontend/src/views/Recommendations.vue)
- [frontend/src/constants/cities.ts](frontend/src/constants/cities.ts)

推荐页当前会判断：

```ts
POPULAR_CITIES.includes(q.trim())
```

如果从城市专题、历史行程或其他入口传入一个不在固定数组中的城市，推荐页会忽略 query 并回到默认城市。

这与代码注释中“城市不在列表也可在输入框直接填”并不完全一致，因为推荐页当前没有真正的城市输入框。

### 建议

二选一：

1. 推荐页增加城市搜索/输入框，允许任意合法城市；
2. 至少接受非空 query，并交给后端城市校验和同步；
3. 如果产品只支持固定城市，就删除“任意城市可输入”的注释和暗示。

推荐长期采用城市搜索框，而不是把城市列表硬编码在前端。

---

## P1-5 首页和发现页的推荐反馈刷新存在“跳回第一页”体验

当前收藏或不感兴趣后，父页面会重新加载第一页推荐：

```text
用户正在看第 2 页
    -> 点击收藏
    -> 重新请求第 1 页
    -> 原来位置消失
```

这不算数据 Bug，但会让用户觉得列表跳动，尤其是加载更多之后。

### 建议

- 第一页反馈：可以重新排序；
- 非第一页反馈：局部更新当前卡片状态，不立即重置列表；
- 需要重新排序时显示“推荐已更新，点击刷新”；
- 保留当前滚动位置。

---

# 五、P2 问题

## P2-1 推荐分数不应该作为用户可见的唯一解释

即使修复匹配度字段，也建议不要只展示百分比。用户更容易理解：

```text
适合慢节奏旅行
匹配你的自然风景偏好
你还没有去过
本地攻略已收录
```

比：

```text
匹配度 86%
```

更可信、更可解释。

推荐卡可以采用：

```text
为什么推荐
[自然风景] [慢节奏]
匹配你的偏好，且你还没有去过
```

百分比可以作为次级信息，甚至首版完全隐藏。

---

## P2-2 “已去过”与“新颖性”文案有轻微矛盾

评分器对已去过的景点降低分数，但推荐理由又会返回：

```text
你曾去过这里，本次为你换点新地方
```

这句话容易让用户疑惑：既然要换新地方，为什么还推荐这个已经去过的地方？

### 建议

二选一：

- 降低已去过景点的曝光，并只在详情页展示“你曾去过”；
- 如果保留在推荐流中，文案改为：`你曾去过，为你保留熟悉选项`，不要说“换点新地方”。

当前 `PersonalizedScoreCalculator` 和 `RecommendationFeedService.reasonOf()` 的文案也要统一。

---

## P2-3 收藏与不感兴趣后的推荐理由需要实时更新

当前卡片内部会更新收藏状态，但推荐流刷新后如果后端画像更新是异步或失败，可能出现：

```text
刚收藏了自然风景
刷新后仍然没有显示命中自然风景
```

建议反馈接口返回：

```json
{
  "success": true,
  "profile_version": 8,
  "message": "已记录，将影响后续推荐"
}
```

前端用版本号判断推荐是否真正刷新，避免只凭成功提示推断画像已经更新。

---

## P2-4 当前测试缺少前端展示层测试

后端已有较多推荐服务测试，覆盖了：

- 无画像降级；
- travel_style 命中；
- 攻略质量排序；
- 景点收藏态；
- 7 天过期刷新；
- 曝光幂等。

但目前缺少能防止本次 Bug 的测试：

- `score=0.5` 且无画像时不能显示匹配度；
- 有兜底理由但没有命中标签时不能显示匹配度；
- `popular/latest` 页面不能显示个性化匹配度；
- 加入行程 query 必须包含景点名称或 ID；
- 规划页接收景点后不能覆盖用户已有备注。

如果暂时不引入完整组件测试，至少把匹配度计算抽成纯函数并单测：

```ts
getVisibleMatchPercent(item, feedPersonalized, sort)
```

---

# 六、建议的目标数据契约

建议把推荐项改成下面这种更明确的结构：

```ts
export interface RecommendationItem {
  spot_id: string;
  poi_id: string;
  name: string;
  city: string;
  score?: number | null;                 // 内部综合排序分，不直接展示
  personalized?: boolean | null;         // 本条是否真实命中个性化
  match_score?: number | null;            // 用户偏好匹配分，0~1
  matched_preferences?: string[] | null; // 命中的偏好标签
  recommend_reason?: string | null;      // 面向用户的解释
}
```

推荐流响应：

```ts
export interface RecommendationFeed {
  items: RecommendationItem[];
  personalized: boolean;
  personalization_domains?: string[];
  profile_version?: number;
}
```

加入行程请求：

```ts
export interface PlanPrefill {
  city: string;
  spot?: string;
  spot_id?: string;
  poi_id?: string;
}
```

第一版可以仍然使用 query 传递，后续再改成显式 `requested_spots`。

---

# 七、建议 Workbuddy 的实现顺序

## 第一批：修复匹配度误导

1. 后端或前端明确区分综合排序分和匹配分；
2. 没有真实画像命中时隐藏“与你的偏好匹配度”；
3. `popular/latest` 排序隐藏个性化百分比；
4. 推荐理由和展示标签统一；
5. 增加无画像、未命中和非个性化排序测试。

## 第二批：修复加入行程闭环

1. 景点卡和详情页传递 `city + spot + spot_id + poi_id`；
2. 规划页显示“已加入景点”；
3. 将景点合并到 `special_notes`，不覆盖用户备注；
4. 提交前允许删除已加入景点；
5. 后端继续复用现有 `collectRequestedSpots()`；
6. 生成结果中校验指定景点是否真的安排；
7. 增加端到端测试。

## 第三批：统一推荐语义

1. 把“高德热门景点”改成“城市精选”或真实热度语义；
2. 把“最新”明确为“最近更新”或改用 `created_at`；
3. 明确景点推荐实际使用的画像域；
4. 增加城市搜索或后端城市校验；
5. 优化反馈后列表不跳页体验。

## 第四批：补前端验证

1. 运行 `npm run build`；
2. 检查推荐卡片在无画像、低画像、强画像下的显示；
3. 检查推荐页切换城市、排序和加载更多；
4. 检查景点卡、详情页和规划页的加入行程链路；
5. 检查移动端不出现按钮遮挡或 query 传参丢失。

---

# 八、可以直接交给 Workbuddy 的执行提示词

请基于 `个性化推荐与加入行程问题排查报告.md` 修复当前前端和后端问题，优先完成 P0 和 P1。

重点要求：

1. 不要再把推荐项的综合排序分 `score` 直接显示为“与你的偏好匹配度”；
2. 只有真正命中用户偏好、且当前推荐流为个性化排序时，才显示匹配度和命中标签；
3. 无画像、未命中、攻略优先、最近更新排序时，不显示虚假的个性化百分比；
4. 景点卡和景点详情的“加入行程”必须携带城市、景点名称和可用的 `spot_id` / `poi_id`；
5. 规划页接收后要显示已加入景点，并合并进特殊要求，不得覆盖用户已有备注；
6. 复用后端现有的 `special_notes -> requestedSpots` 能力，确保模型知道该景点是用户明确指定的地点；
7. 同一个景点重复进入规划页时要去重；
8. 统一“城市精选、攻略收录、最近更新、个性化推荐”等文案，不能把质量排序说成热门，也不能把综合分说成匹配度；
9. 保留现有 JWT、路由、推荐接口、反馈接口和行程生成协议，除非为了补充明确字段确实需要扩展 DTO；
10. 增加必要的后端单测和前端纯函数/组件测试；
11. 完成后运行后端测试、`npm run build`，并汇报每个问题的修复文件和验证结果。

---

# 九、最终验收标准

## 推荐展示

- 无画像用户不会看到“与你的偏好匹配度 50%”；
- 没有命中当前景点时不会显示个性化百分比；
- 只有真正个性化排序且有命中标签时显示匹配度；
- 匹配度分数和文案含义一致；
- 推荐理由能说明“为什么推荐”，而不是只给一个数字；
- 攻略优先、最近更新、城市精选不会伪装成个性化推荐。

## 加入行程

- 从景点卡点击后，规划页目的地正确填入城市；
- 规划页同时显示具体加入的景点；
- 用户提交后，后端能识别该景点为明确指定地点；
- 生成结果尽量安排该景点，无法安排时要明确提示；
- 用户已有特殊要求不会被覆盖；
- 重复点击同一景点不会产生重复文本；
- 从景点详情页进入时行为与景点卡一致。

## 工程质量

- 后端测试通过；
- `npm run build` 通过；
- 不改变现有登录和鉴权行为；
- 不引入新的 API 字段歧义；
- 前端推荐卡在空数据、失败、无画像、弱画像、强画像下都有合理状态；
- 移动端按钮、标签和已加入景点区域不溢出。

---

## 十、结论

当前推荐系统的核心算法和数据底座已经有较好基础，问题主要发生在“内部算法字段直接暴露给用户”和“推荐交互没有把上下文传完整”这两个边界上。

最重要的修复原则是：

```text
综合排序分不等于用户匹配度
有推荐理由不等于命中了用户偏好
点击加入行程不等于只填入城市
```

Workbuddy 本轮应优先修复这三个语义问题。修复后，推荐系统会从“看起来有个性化”变成“展示内容与真实个性化逻辑一致”，加入行程也会从简单跳转变成真正可用的规划入口。
