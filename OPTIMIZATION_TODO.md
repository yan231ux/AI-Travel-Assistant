# 个性化规划系统后续优化清单

## 一、当前完成情况

当前系统已经完成：

- 结构化用户画像；
- 偏好问卷；
- 收藏和不感兴趣反馈；
- 用户偏好权重更新；
- 个性化候选排序；
- 历史景点新颖性降权；
- 推荐日志和候选证据；
- 个性化推荐理由；
- 结果页完整时间轴；
- 每日费用、预算健康度和数据可信度统计；
- 个性化规划摘要；
- “为什么没有推荐”的部分解释；
- 流式和非流式生成统一收尾；
- 个性化相关测试和前端构建验证。

当前版本已经具备答辩展示价值，后续重点是修正细节和补足证据闭环。

## 二、必须优先修复

### 1. 修正每日时间轴顺序

当前结果页中的景点和交通先加入时间轴，餐饮统一追加到后面，因此餐饮可能不符合真实时间顺序。

建议：

- 为 `MealItem` 增加 `start_time` 字段；
- 早餐、午餐、晚餐由结构化字段表示时间段；
- 前端统一按分钟数排序；
- 缺少时间时放入“其他安排”，禁止猜测时间。

验收示例：

```text
09:00 景点 A
12:00 午餐
14:00 景点 B
18:00 晚餐
```

不能出现餐饮全部排在景点之后的情况。

### 2. 统一候选证据和最终结果的关联方式

当前候选是否最终入选主要依赖名称精确匹配。景点被替换、名称存在别名或 POI 名称变化时，可能导致 `selected` 判断错误。

建议优先使用：

```text
poi_id
    -> 城市 + 标准化名称
    -> 经纬度近似匹配
    -> 名称匹配兜底
```

验收要求：

- 候选被校验层替换后，证据状态同步更新；
- 最终行程中的真实 POI 能正确关联候选证据；
- 推荐日志和候选证据中的景点状态一致。

### 3. 补充过滤原因

当前“为什么没有推荐”主要解释：

- 用户近期不感兴趣；
- 历史行程中出现过。

建议继续记录以下确定性原因：

- 恶劣天气，不适合户外活动；
- 距离或交通时间过长；
- 超出预算；
- 用户少走路或不早起约束；
- 营业时间冲突；
- 饮食禁忌不匹配。

推荐结构：

```json
{
  "name": "某户外景区",
  "reason": "当天存在大雨，不安排户外活动",
  "evidence": "WEATHER_API",
  "severity": "HARD"
}
```

## 三、建议增强

### 4. 统一个性化评分口径

候选排序和推荐日志应始终复用同一个评分组件，例如：

```text
PersonalizedScoreCalculator
```

统一输出：

```text
preference_score
behavior_score
novelty_score
distance_penalty
final_score
matched_tags
avoid_reason
```

当前距离惩罚仍为预留值 `0.0`。后续可以根据高德路线的距离和时间，对具体日程中的相邻景点计算交通惩罚。

### 5. 区分具体地点反馈和类别反馈

用户不喜欢一个景点，不一定代表用户不喜欢该景点所属的全部标签。

建议在“不感兴趣”后提供原因：

- 不喜欢这个地点；
- 不喜欢这类景点；
- 距离太远；
- 太拥挤；
- 价格太高；
- 不适合当前节奏；
- 其他。

只有用户明确选择“不喜欢这类景点”时，才降低对应标签权重。

### 6. 完善收藏逻辑

收藏应同时保存：

- 具体 POI；
- POI 所属偏好标签；
- 城市；
- 当时的旅行上下文。

后续应优先推荐相似但未体验过的新地点，而不是机械重复收藏过的地点。

### 7. 增加推荐版本信息

推荐日志和候选证据建议记录：

```text
profile_version
ranking_version
model_version
data_snapshot_time
```

这样可以解释不同版本算法产生的结果，也方便做对照实验。

### 8. 可信度字段结构化

当前前端主要通过 `source` 文本判断可信度。长期建议后端增加结构化字段：

```json
{
  "source": "高德POI",
  "evidence_type": "AMAP_POI",
  "verified_fields": ["name", "address", "latitude", "longitude"]
}
```

避免前端依赖中文字符串判断数据类型。

## 四、结果页建议保留的内容层级

结果页应明确区分四类信息：

### 已验证事实

- 景点名称；
- 餐厅名称；
- 地址和坐标；
- 高德路线距离和时长；
- 天气；
- 攻略卡片门票。

### 系统计算

- 总预算；
- 每日预算；
- 预算使用率；
- 景点数量；
- 交通总距离和耗时；
- 个性化得分；
- 推荐命中率。

### 模型建议

- 行程主题；
- 景点介绍；
- 游览建议；
- 旅行提示。

### 待核实内容

- 未命中 POI 或攻略的名称；
- 没有真实来源的价格；
- 无法匹配的地址；
- LLM 估算的交通信息。

模型生成但没有真实证据的内容必须标注“模型建议”或“需核实”。

## 五、推荐实施顺序

```text
P0：修正餐饮时间轴顺序
    -> P0：统一 POI/名称关联
    -> P0：补充天气、距离、预算过滤原因
    -> P1：完善反馈原因和收藏上下文
    -> P1：补充推荐版本信息
    -> P2：加入距离惩罚和更细的推荐评分
    -> P2：完善可信度结构化字段
```

## 六、验收标准

### 功能验收

- 每日所有景点、餐饮、交通、住宿和备注都能展示；
- 时间轴顺序与结构化时间一致；
- 推荐理由和过滤原因来自确定性数据；
- 用户反馈后画像版本发生变化；
- 用户反馈后相同参数不会命中旧的个性化缓存；
- 流式和非流式接口返回相同的个性化字段。

### 可信度验收

- 地址、坐标、路线和天气有明确来源；
- 未验证内容明确标记；
- 总预算和每日预算由代码计算；
- 页面不展示 LLM 编造的预算核算；
- 过滤原因能指向具体证据来源。

### 实验验收

至少保留以下三种方案进行对比：

```text
无个性化
Prompt 历史画像
完整个性化排序与反馈闭环
```

统计：

- 偏好命中率；
- 负反馈率；
- 重复推荐率；
- 约束满足率；
- 平均满意度；
- 推荐理由解释接受度。

## 七、当前结论

当前版本已经不是简单的“LLM 生成旅游攻略”，而是具备：

```text
用户画像
    -> 候选排序
    -> Agent 规划
    -> 事实校验
    -> 推荐解释
    -> 用户反馈
```

后续不需要继续无目的增加 LLM 文案，优先完善时间轴、证据关联、过滤原因和反馈粒度。这些改动对系统可信度、结果页质量和毕设答辩价值提升最大。

## 八、实测问题登记区（2026-09-07 起，只登记不改，等汇总完统一修）

> **规则**：对真实生成结果做人工评审发现的确定性缺陷，一律登记到本节（现象 / 根因 / 建议方向 / 证据出处 / 状态）。
> 登记 ≠ 立即修：攒够一批后统一评估修复（避免边评边改打乱主线）。修复时在本节把"状态"改为"已修复+日期"。

### Q1. 餐厅候选无区域/距离约束，出现"跨区吃午饭"式行程矛盾
- **现象**（trip_西安_2026-09-07）：
  - D2 上午在临潼兵马俑（距市区 43km），12:45 午餐却安排市区大雁塔附近的"大秦御鼎火锅(博乐里购物广场店)"，吃完 14:00 又回临潼华清宫——物理上不可能；
  - D1 午餐"福苗小骆驼(凤八店)"、D3 晚餐"蛮涮自助(汉神广场店)"均在北郊凤城路一带，与当天城墙/大雁塔活动区脱节。
- **根因**：餐厅候选池只按"火锅/烧烤"等标签召回，**没有当日景点活动区域的距离过滤/距离排序**；LLM 从候选里挑店时不掌握空间合理性，模型自身又倾向于"看着顺眼"的店名。`distance_penalty` 字段一直预留 0.0 未接通。
- **建议方向**（修复时评估）：①候选阶段用当日景点锚点（如当天市中心/首景点坐标）做距离过滤或排序，超阈值餐厅降级；②提示词增加"餐厅须靠近当日活动区域"约束并给候选距离证据；③真正接通 distance_penalty（OPTIMIZATION_TODO §4 已有铺垫）。
- **证据出处**：行程 JSON 中 D2 午餐/晚餐 poi 与当日景点坐标对比；候选证据 candidate_evidence 的 poi_id/经纬度已具备，可支撑距离计算。
- **状态**：待修复（登记于 2026-09-07）

### Q2. 跨区交通费漏算，预算被低估
- **现象**：D2"酒店→兵马俑 43.4km 打车"仅有距离与时长、**无金额**；华清宫（临潼）返市区晚餐/酒店也无返程计费。预算"交通费用 ¥35"严重偏低，总费用 ¥1969（预算使用率 131%）实际是低估后的数字。
- **根因**：交通段金额来源不一——高德路线估算有的段计价、有的段只给距离；LLM 估算段（¥0/¥5/¥30）不成体系；跨区大段（打车 40km+）未被正确计入交通汇总。
- **建议方向**：修复时统一"交通段必须带金额"口径（高德驾车计价 or LLM 估算并标橙），并校验每日交通合计与明细一致；跨区段缺失时收尾兜底补算。
- **证据出处**：行程 JSON 每日交通段与预算明细"交通 ¥35"对比。
- **状态**：待修复（登记于 2026-09-07）

### Q3. 预算超标只提示不收敛
- **现象**：用户画像预算 ¥1500，实际 ¥1969（131%），页面提示"超出预算/剩余 ¥0"但行程未做任何调整；酒店 ¥400/晚 × 3 = ¥1200 占 61% 是超支主因。
- **根因**：预算只在结果页做"健康度展示"，生成阶段没有按预算反向约束（如根据预算反推酒店档次/景点取舍）。
- **建议方向**：修复时评估"生成后预算收敛"（超支时先降酒店档次/砍非核心景点，再提示）或至少"预算敏感字段进提示词让 LLM 一开始就收敛"。
- **证据出处**：trip_西安_2026-09-07 预算明细 vs user_profile.budget_preference。
- **状态**：待修复（登记于 2026-09-07）

### Q4. 西安大景点未命中攻略卡片（门票标成"LLM 建议"）
- **现象**：兵马俑博物馆（门票 ¥120）、华清宫（¥120）在结果页标为"LLM 建议（需核实）"而非"本地攻略"，尽管票价正确——说明攻略卡片没匹配上。
- **根因**：待核——西安攻略 md 是否收录兵马俑/华清宫卡片；若收录，则是匹配键（景点名/别名）不一致导致未命中。
- **建议方向**：核对西安 guides 内容与景点名匹配规则（别名表是否需要补"兵马俑博物馆/秦始皇帝陵博物院"这类映射）。
- **证据出处**：trip_西安_2026-09-07 门票来源徽标；backend guides/西安攻略文件。
- **状态**：待修复（登记于 2026-09-07，需先人工核对 md 是否缺条目再定性）

### Q5. ProfileView "来自你的历史行程" 卡片显示与实际不符（5 次 vs 11 次 / 4 城 vs 8 城）
- **现象**（2026-09-07 截图）：ProfileView 顶部"来自你的历史行程"卡显示"5 次历史行程 · 去过 大理/杭州/西安/丽江（4 个城市）"，但 HistoryView 实际有 11 张行程卡片，DB trip_record user_id=3 共 11 条，去重目的地 8 个（大理/杭州/西安/丽江/上海/厦门/重庆/大连）。
- **根因**：`ProfileView.vue:187/193` 直接读 `user_profile` 里的 `trip_count`（=5）和 `visited_cities`（=4 个），这两个字段是 2026-09-04 12:58:52 创建画像时的快照，**之后每生成新行程没有联动更新**——所以一直显示 4 城 5 次。
- **建议方向**（修复时评估，二选一）：
  - **A. 写库侧同步**（推荐）：`TripRecordService` 写库成功后触发 `UserProfileService.refreshStats(userId)` 实时从 trip_record 聚合重算 `trip_count` 和 `visited_cities` 并更新画像表。优点：单一事实源，画像表始终准。
  - **B. 前端实时聚合**：新增 `GET /user/profile/summary` 接口，后端 `SELECT COUNT(*) + SELECT DISTINCT destination FROM trip_record WHERE user_id=? AND deleted=0`，前端 ProfileView 调这个接口。优点：彻底解耦，画像表不再存派生数据。
- **证据出处**：ProfileView.vue:187 `visitedCities = computed(() => splitList(profile.value?.visitedCities))`、:193 `p.tripCount && p.tripCount > 0 ? parts.push(\`${p.tripCount} 次历史行程\`)`；DB 中 user_profile.trip_count=5、visited_cities='大理,杭州,西安,丽江'、updated_at=2026-09-04 12:58:52；trip_record 11 条实际数据。
- **修复结果**：✅ 已修复（2026-09-07 批次 D 落地**方案 B**）——后端新增 `UserProfileService.buildProfileSummary()` 基于未删除 trip_record **全量实时聚合** tripCount/visitedCities（不受画像推断 RECORD_LIMIT 限制），`GET /user/profile/summary` 提供；前端 ProfileView（及新首页画像摘要卡）一律改用 summary 实时数据，user_profile 快照字段保留作历史推断输入、不再作展示事实源。详见 ProductEvolution 批次 D。
- **状态**：已修复（2026-09-07）

### 附：本次实测确认正常（答辩可引用，非问题）
trip_西安_2026-09-07（2026-09-07 生成）完整跑通：偏好命中徽标（火锅/烧烤）、历史重复景点降级并给来源（大明宫/回民街 📎 历史行程）、ReAct 4 步推理回放、收尾摘要、过滤说明面板、可信度分层（高德 POI 6/攻略 4/模型建议 6/路线估算 2、地址校验 5/6、自动发现大唐不夜城地址疑似串用并标待核实）。
